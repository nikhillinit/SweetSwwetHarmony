# Discovery Engine - MCP Server Dependencies

# MCP SDK (Model Context Protocol)
mcp>=1.0.0

# HTTP clients (async)
httpx>=0.25.0
aiohttp>=3.9.0

# Retry logic
tenacity>=8.2.0

# Environment variables
python-dotenv>=1.0.0

# Type validation
pydantic>=2.0.0
instructor>=1.14.4

# Async SQLite for signal storage
aiosqlite>=0.19.0

# Date parsing (for WHOIS/domain data)
python-dateutil>=2.8.0

# HTML to text extraction (for URL profiler)
trafilatura>=1.6.0

# HTML parsing with CSS/XPath selectors (monitoring content pipeline)
parsel>=1.8.0

# HTML to text with layout preservation (monitoring content pipeline)
inscriptis>=2.5.0

# Structured data extraction (JSON-LD, microdata, OpenGraph)
extruct>=0.17.0

# NLP / Medical entity extraction
scispacy>=0.5.0
spacy>=3.5.0

# NER model for company name extraction (pinned to match spacy>=3.5.0)
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1.tar.gz

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
respx>=0.22.0

# Notion SDK
notion-client>=2.0.0

# Rich terminal output
rich>=13.0.0

# API Layer (FastAPI backend)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pyjwt>=2.8.0
python-multipart>=0.0.6

# Email Distribution
jinja2>=3.1.0
resend>=2.0.0

# Dashboard
streamlit>=1.30.0
pandas>=2.0.0
altair>=5.0.0

# LLM (Gemini) — new unified SDK (replaces legacy google-generativeai)
google-genai>=1.0.0

# PDF Profiler (Phase 1)
pymupdf>=1.23.0
pdfplumber>=0.11.0

# Curated Discovery (Phase 2)
tavily-python>=0.3.0

# Evaluation Framework (Inspect AI)
inspect-ai>=0.3.0
inspect-flow>=0.1.0

# Domain extraction (canonical_key_v2)
tldextract>=5.0.0

# Optional: curl-cffi for monitoring transport (lazy import in monitoring/content_pipeline/transport_curl.py)
# curl-cffi>=0.7.0

# Phase G: Identity Resolution
rapidfuzz>=3.0.0
metaphone>=0.6

# Phase 0A: Policy Loading
pyyaml>=6.0

# Phase 3: Pipeline Scheduler
croniter>=2.0.0

# ML Thesis Model (supervised classifier for false-negative rescue)
scikit-learn>=1.3.0
joblib>=1.3.0

---
phase: 01-move-0-prep-liveness-prep
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/discovery-pipeline.yml
autonomous: true
requirements: [LIV-11]
tags: [ci, github-actions, r19-ci-lane, optional]

must_haves:
  truths:
    - "The 5 occurrences of `-v` in `.github/workflows/discovery-pipeline.yml` (lines 108, 117, 138, 155, 157) are removed"
    - "The CI workflow's next scheduled run completes without `unrecognized arguments: -v` errors (verifiable post-merge via gh run list)"
    - "This is the SAME R19 failure mode in a different lane: silent failure with stale data — fixing it brings the CI lane back into operational state"
    - "This plan is OPTIONAL per CONTEXT.md scope; it lives in an allowed path (.github/workflows/) and is recommended by RESEARCH.md Finding 2"
    - "If the executor or user prefers to defer this fix, the plan can be skipped without affecting any other Phase 1 plan; document the deferral in plan 01-10's VERIFICATION.md"
  artifacts:
    - path: ".github/workflows/discovery-pipeline.yml"
      provides: "CI pipeline workflow with -v flag removed from 5 lines"
  key_links:
    - from: ".github/workflows/discovery-pipeline.yml"
      to: "run_pipeline.py argparse"
      via: "workflow invokes run_pipeline.py subcommands; -v was removed from argparse at some point and the workflow was never updated"
      pattern: "run_pipeline.py"
---

<objective>
Fix the 5+ day silent failure in `.github/workflows/discovery-pipeline.yml`. The workflow has been failing every day since 2026-04-03 with `run_pipeline.py: error: unrecognized arguments: -v`. This is the SAME failure mode as R19 (silent freeze with stale data) in a different lane (CI vs local production DB). RESEARCH.md Finding 2 recommends this as an optional Wave A sub-task.

This plan is OPTIONAL — CONTEXT.md does not include it in the original scope. The user / orchestrator may choose to defer it. If included, it costs <10 minutes of executor time and produces a clean CI lane that mirrors the local production restart of LIV-01.

Per RESEARCH.md Finding 2, the `-v` flag was removed from `run_pipeline.py` argparse at some point but `.github/workflows/discovery-pipeline.yml` was never updated. The fix is removing 5 occurrences of ` -v` (leading space + flag) across 5 lines.

Output:
1. `.github/workflows/discovery-pipeline.yml` with the 5 `-v` removals
2. (Optional verification, post-merge): the next scheduled CI run no longer errors on argparse
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-move-0-prep-liveness-prep/1-CONTEXT.md
@.planning/phases/01-move-0-prep-liveness-prep/1-RESEARCH.md
@.github/workflows/discovery-pipeline.yml

<interfaces>
<!-- Verified line numbers per RESEARCH.md §"Daily Pipeline failure history" + direct file read -->

```yaml
# Line 108 (Sync portfolio companies step)
python run_pipeline.py monitor sync-portfolio -v || echo "Portfolio sync completed with warnings"

# Line 117 (Run portfolio monitoring step)
python run_pipeline.py monitor run --only-portfolio --limit 100 --no-embeddings -v || true

# Line 138 (Sync suppression cache from Notion step)
python run_pipeline.py sync -v || echo "Suppression sync completed with warnings"

# Lines 155, 157 (Run discovery collectors step, two branches of the dry-run conditional)
python run_pipeline.py full --collectors "$COLLECTORS" --dry-run -v
python run_pipeline.py full --collectors "$COLLECTORS" -v
```

<!-- Verified failure root cause per gh run list / gh run view 24068064584 --log-failed -->

```
run_pipeline.py: error: unrecognized arguments: -v
##[error]Process completed with exit code 2.
```

<!-- .github/workflows/ is in the ALLOWED Move 0 path list per check_protected_paths.sh -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove 5 -v flag occurrences from discovery-pipeline.yml</name>
  <files>.github/workflows/discovery-pipeline.yml</files>
  <read_first>
    - .github/workflows/discovery-pipeline.yml (full file — confirm the exact lines/columns of each -v occurrence; line numbers in the plan may have drifted slightly if the file changed since RESEARCH.md was written)
    - .planning/phases/01-move-0-prep-liveness-prep/1-RESEARCH.md §"Daily Pipeline failure history" (failure mode + line numbers from gh run view)
  </read_first>
  <action>
    Open `.github/workflows/discovery-pipeline.yml`. Locate every occurrence of ` -v ` or ` -v$` (leading space + literal `-v` + trailing space-or-end-of-line) on lines that invoke `run_pipeline.py`. Per RESEARCH.md Finding 2 and the verified file at the time of plan writing, there are exactly 5 such occurrences at lines 108, 117, 138, 155, and 157.

    Remove the ` -v` from each:

    **Line 108 — Sync portfolio companies**

    Before:
    ```yaml
    python run_pipeline.py monitor sync-portfolio -v || echo "Portfolio sync completed with warnings"
    ```

    After:
    ```yaml
    python run_pipeline.py monitor sync-portfolio || echo "Portfolio sync completed with warnings"
    ```

    **Line 117 — Run portfolio monitoring**

    Before:
    ```yaml
    python run_pipeline.py monitor run --only-portfolio --limit 100 --no-embeddings -v || true
    ```

    After:
    ```yaml
    python run_pipeline.py monitor run --only-portfolio --limit 100 --no-embeddings || true
    ```

    **Line 138 — Sync suppression cache from Notion**

    Before:
    ```yaml
    python run_pipeline.py sync -v || echo "Suppression sync completed with warnings"
    ```

    After:
    ```yaml
    python run_pipeline.py sync || echo "Suppression sync completed with warnings"
    ```

    **Line 155 — Run discovery collectors (dry-run branch)**

    Before:
    ```yaml
    python run_pipeline.py full --collectors "$COLLECTORS" --dry-run -v
    ```

    After:
    ```yaml
    python run_pipeline.py full --collectors "$COLLECTORS" --dry-run
    ```

    **Line 157 — Run discovery collectors (live branch)**

    Before:
    ```yaml
    python run_pipeline.py full --collectors "$COLLECTORS" -v
    ```

    After:
    ```yaml
    python run_pipeline.py full --collectors "$COLLECTORS"
    ```

    DO NOT modify any other lines. DO NOT change the workflow's cron schedule, env vars, secrets references, or step names. The diff should show exactly 5 lines changed, each removing ` -v` (3 characters total per line: space, dash, v).

    **Sanity check before commit:** verify no other ` -v ` patterns exist in the file that might be unrelated args (e.g., `--verbose` is fine; only the bare `-v` short flag is the bug). Run:

    ```bash
    grep -n " -v " .github/workflows/discovery-pipeline.yml
    grep -n " -v$" .github/workflows/discovery-pipeline.yml
    # Both should return zero matches after the edit
    ```

    If line numbers have drifted (e.g., the file was edited since RESEARCH.md captured them), find the offending lines by their context: each is a `python run_pipeline.py <subcommand> ... -v` invocation under `run: |` blocks.
  </action>
  <verify>
    <automated>grep -c " -v " .github/workflows/discovery-pipeline.yml; grep -c " -v$" .github/workflows/discovery-pipeline.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c " -v " .github/workflows/discovery-pipeline.yml` returns `0` (no leading-space `-v ` followed by space)
    - `grep -c " -v$" .github/workflows/discovery-pipeline.yml` returns `0` (no trailing `-v` at end of line)
    - `grep -c " -v\\\\$" .github/workflows/discovery-pipeline.yml` returns `0` (no `-v \\` line continuation)
    - `grep -c "python run_pipeline.py" .github/workflows/discovery-pipeline.yml` is unchanged from the pre-edit count (the `python run_pipeline.py` invocations still exist; only their `-v` argument is removed)
    - Git diff is exactly 5 line changes, all removing ` -v` (or `-v `) substrings
    - `git diff .github/workflows/discovery-pipeline.yml | grep "^-" | grep -c "\\-v" ` returns at least `5` (5 lines had `-v` removed)
    - `bash scripts/red-team-hybrid/check_protected_paths.sh` rc=0 (`.github/workflows/` is allowed)
    - YAML still parses (no structural damage): `python -c "import yaml; yaml.safe_load(open('.github/workflows/discovery-pipeline.yml'))"` exits 0 (PyYAML is in stdlib for this check via `pip show pyyaml`; if not installed, fall back to `python -c "import json, subprocess; subprocess.check_call(['gh', 'workflow', 'view', 'discovery-pipeline.yml', '--ref', 'HEAD'])"` post-commit, OR skip the YAML parse check and rely on the next CI run)
  </acceptance_criteria>
  <done>5 -v flags removed; YAML still valid; no other workflow content changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → git tree | Single YAML file edit in `.github/workflows/`. Allowed Move 0 path. |
| Workflow → CI runner | Post-merge, the workflow runs on GitHub-hosted runner. CI runner trust boundary unchanged (no new secrets, no new env vars). |
| CI workflow → signals.db (artifact) | Workflow uses `signals-db-latest` artifact, NOT the local production DB per RESEARCH.md Finding 1. The CI DB is a separate instance; this fix does not affect local production data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | YAML structure damage | mitigate | Edit removes only ` -v` substrings; YAML parse check in acceptance criteria. |
| T-11-02 | Denial of service | Removing -v breaks logging that downstream parsers depend on | accept | RESEARCH.md confirms `run_pipeline.py` argparse no longer recognizes `-v` — there's nothing to log to remove. The flag was already non-functional (causing exit 2), so removing it cannot remove behavior. |
| T-11-03 | Elevation of privilege | New permissions added | mitigate | Edit is purely subtractive; no new permissions, env vars, or secrets. |
| T-11-04 | Spoofing | Workflow now succeeds when it shouldn't | accept | Workflow now correctly invokes commands without the bad flag. Whether the workflow succeeds end-to-end is a separate downstream question (and Finding 1 already established the CI DB is divergent regardless). |

<data_integrity>
N/A — pure YAML edit. The CI DB is a separate instance from local production per RESEARCH.md Finding 1; this fix does not affect local data integrity.
</data_integrity>
<supply_chain>
No new secrets, no new env vars, no new actions, no new packages.
</supply_chain>
<protected_paths>
`.github/workflows/` is in the ALLOWED Move 0 path list per `check_protected_paths.sh`. Verified by RESEARCH.md §"`.github/workflows/` inventory".
</protected_paths>
<secrets>
N/A.
</secrets>
<mitigations>
- `check_protected_paths.sh` pre-commit guard
- YAML parse check in acceptance criteria
- Diff is purely subtractive (5 line edits, each removing ` -v`)
- No new permissions or secrets
</mitigations>
</threat_model>

<verification>
This plan is OPTIONAL per CONTEXT.md scope. If executed, it satisfies an implicit Hard Rubric Gate (the CI lane is no longer silently failing the same R19 failure mode). If skipped, plan 01-10's VERIFICATION.md must record the deferral with a Phase 2 carry-over.

Verification commands (post-edit):
```bash
grep -c " -v " .github/workflows/discovery-pipeline.yml  # expect: 0
grep -c " -v$" .github/workflows/discovery-pipeline.yml  # expect: 0
grep -c "python run_pipeline.py" .github/workflows/discovery-pipeline.yml  # expect: unchanged from pre-edit
bash scripts/red-team-hybrid/check_protected_paths.sh  # expect: rc=0
```

Verification commands (post-merge, deferred until next CI run):
```bash
gh run list --workflow=discovery-pipeline.yml --limit 1
# expect: latest run is "success" or at least past the argparse-error step
gh run view <latest_run_id> --log | grep "unrecognized arguments: -v"
# expect: no matches
```
</verification>

<success_criteria>
- 5 `-v` flags removed from discovery-pipeline.yml
- YAML still parses
- No other workflow content changed
- Pre-commit gate passes
- (Post-merge) next scheduled CI run no longer errors on argparse
- This R19-shaped CI-lane failure is closed
</success_criteria>

<output>
After completion, create `.planning/phases/01-move-0-prep-liveness-prep/01-11-SUMMARY.md` capturing:
- Commit SHA
- Pre/post grep counts of ` -v` patterns
- git diff stat (5 lines changed)
- (If accessible at execution time) result of next CI run via `gh run list --workflow=discovery-pipeline.yml --limit 1`
- Confirmation .github/workflows/ is the allowed path used
</output>
</content>
</invoke>
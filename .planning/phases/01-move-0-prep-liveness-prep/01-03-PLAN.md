---
phase: 01-move-0-prep-liveness-prep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/plans/2026-04-06-red-team-hybrid/05-holdout-cohort-design.md
  - docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md
autonomous: true
requirements: [GOV-01]
tags: [docs, governance, bias-audit, withdrawal]

must_haves:
  truths:
    - "The '9% pipeline precision' claim at 05-holdout-cohort-design.md:44 is wrapped in a dated strike-through caveat referencing the 2026-04-06 bias audit"
    - "The '9% pipeline precision' claim at 06-tier-2-recall-eval.md:74 is wrapped in a dated caveat"
    - "§11 of 06-tier-2-recall-eval.md is REWRITTEN (not struck through) per D-25 to frame Tier-2 recall as the primary metric and explicitly retire 9% as a comparison target"
    - "A wider grep of 9% across docs/ + .planning/ + CLAUDE.md confirms only the 4 target lines needed edits; all other '9%' matches are already wrapped in withdrawal framing or are read-only audit references per RESEARCH.md Finding 3"
    - "A reviewer can see the withdrawal context inline without needing to cross-reference the bias audit"
  artifacts:
    - path: "docs/plans/2026-04-06-red-team-hybrid/05-holdout-cohort-design.md"
      provides: "Dated caveat at line 44 per D-14 pattern"
      contains: "Withdrawn 2026-04-08 (GOV-01)"
    - path: "docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md"
      provides: "Caveat at line ~74 + rewritten §11"
      contains: "Withdrawn 2026-04-08 (GOV-01)"
  key_links:
    - from: "docs/plans/2026-04-06-red-team-hybrid/05-holdout-cohort-design.md"
      to: "docs/plans/2026-04-06-lob-progress-eval/bias-audit.md"
      via: "caveat explicitly cites the bias audit file path"
      pattern: "bias-audit.md"
    - from: "docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md"
      to: "docs/plans/2026-04-06-lob-progress-eval/bias-audit.md"
      via: "caveat explicitly cites the bias audit file path"
      pattern: "bias-audit.md"
---

<objective>
Execute the 9% pipeline precision withdrawal across the 4 known locations in the red-team-hybrid strategy docs, and rewrite §11 of `06-tier-2-recall-eval.md` to frame Tier-2 recall as the primary substrate-quality metric (replacing the 9% comparison baseline per D-25).

Purpose: Post-bias-audit, the 9% number is selection bias per the 2026-04-06 audit. Treating it as a baseline for Tier-2 recall comparison would re-introduce the bias into the new measurement regime. §11 specifically is load-bearing because it was written as the bridge between "old 9% precision" and "new Tier-2 recall" — that bridge needs to be rebuilt around the correct claim (Tier-2 recall is the primary metric, precision is unknown).

Output: Two modified files. Inline dated caveats at `05-holdout-cohort-design.md:44` and `06-tier-2-recall-eval.md:74`. Full rewrite of `06-tier-2-recall-eval.md` §11 (lines 265-298 area).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-move-0-prep-liveness-prep/1-CONTEXT.md
@.planning/phases/01-move-0-prep-liveness-prep/1-RESEARCH.md
@docs/plans/2026-04-06-red-team-hybrid/05-holdout-cohort-design.md
@docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md
@docs/plans/2026-04-06-lob-progress-eval/bias-audit.md

<interfaces>
<!-- Current 9% claim locations (verified 2026-04-08) -->

**Target 1: `docs/plans/2026-04-06-red-team-hybrid/05-holdout-cohort-design.md:42-46`**
```
to classify individual signals. The 9% precision metric (per LOB.txt
evaluation) is on signals; the metric the strategy needs to move is on
companies that turn into meetings/fundings.
```

**Target 2: `docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md:74`**
```
| Precision-on-pushed | TPs / (TPs + FPs) on the train cohort | Sanity check against existing ~9% number |
```

**Target 3: `docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md:265-285` (§11)**
```
## 11. The relationship to the existing 9% precision number

Per LOB.txt evaluation (Phase 0): the existing 211 labels show ~9% precision
on what the pipeline surfaces. **Is Tier-2 going to land near 9%?**

Probably not directly. The two metrics measure different things:
- 9% = TP rate among signals pushed to Notion (precision of the surfaced set)
- Tier-2 recall = fraction of *companies* that turned into outcomes that the
  engine surfaced (recall over outcome-bearing entities)

A high Tier-2 recall with a low precision is consistent ("we surface lots of
things including the right ones, just buried in noise"). A low Tier-2 recall
with a high precision is also consistent ("we surface very few things and they
are mostly right but we miss most of the wins").

The strategy's job is to move BOTH numbers in the right direction, but they
move via different levers:
- Precision improves when classification gets better (Tier-1 work, prompt tuning)
- Recall improves when the substrate captures more, dedups better, holds
  fewer false negatives, and the labelling cohort gets bigger (Tracks B/D/E)
```

**Reference (read-only, DO NOT EDIT): `docs/plans/2026-04-06-lob-progress-eval/bias-audit.md`** — this is the source-of-truth audit. The caveats in the red-team-hybrid docs cite this file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run wider 9% grep per D-26 and capture full match list</name>
  <files>artifacts/gov-01/grep_9pct_sweep.txt</files>
  <read_first>
    - .planning/phases/01-move-0-prep-liveness-prep/1-CONTEXT.md (D-25, D-26 for targets and the "wider sweep" requirement)
    - .planning/phases/01-move-0-prep-liveness-prep/1-RESEARCH.md §"Sources → Grep results" (D-26 sweep already captured; this task re-verifies)
  </read_first>
  <action>
    Create the directory `artifacts/gov-01/` if it does not exist. Run the wider grep sweep per D-26 and save the output to `artifacts/gov-01/grep_9pct_sweep.txt`:

    ```bash
    mkdir -p artifacts/gov-01
    grep -rn "9%" docs/ .planning/ CLAUDE.md > artifacts/gov-01/grep_9pct_sweep.txt 2>&1 || true
    ```

    Open `artifacts/gov-01/grep_9pct_sweep.txt` and classify each match into one of these categories (annotate inline as `# EDIT`, `# READ-ONLY`, or `# UNRELATED`):

    1. **EDIT** — the 3 target lines from D-25: `05-holdout-cohort-design.md:44`, `06-tier-2-recall-eval.md:74`, `06-tier-2-recall-eval.md:265-285` (§11)
    2. **READ-ONLY** — lines in `docs/plans/2026-04-06-lob-progress-eval/bias-audit.md` (this is the audit itself; editing would corrupt the audit trail) and `docs/plans/2026-04-06-lob-progress-eval/README.md:11` (already wrapped in withdrawal context per RESEARCH.md Finding 3)
    3. **UNRELATED** — matches like "89%", "98%", "99%" (substring false-positives), HN FP rate 98.69%, similarity scores, coverage stats

    Per RESEARCH.md Finding 3, the D-25 target list of 4 edit-lines across 3 files is correct. If this grep finds new EDIT-class matches not in D-25, STOP and surface the discrepancy before proceeding to Task 2 (the gate would be a human decision on how to handle the new lines).

    If the grep output matches RESEARCH.md Finding 3's matrix (6 lines across 5 files total; 4 lines in 3 files are edit targets), proceed to Task 2.

    Commit the grep sweep artifact: `git add artifacts/gov-01/grep_9pct_sweep.txt`. It serves as audit evidence of D-26 compliance.
  </action>
  <verify>
    <automated>test -f artifacts/gov-01/grep_9pct_sweep.txt && grep -c "EDIT\|READ-ONLY\|UNRELATED" artifacts/gov-01/grep_9pct_sweep.txt</automated>
  </verify>
  <acceptance_criteria>
    - `test -f artifacts/gov-01/grep_9pct_sweep.txt` → exit 0
    - Sweep file contains at minimum the 3 EDIT-class target lines (`05-holdout-cohort-design.md:44`, `06-tier-2-recall-eval.md:74`, `06-tier-2-recall-eval.md:265`)
    - Sweep file contains at minimum the 4 READ-ONLY-class bias-audit.md lines (at lines 57, 61, 78, 182 per RESEARCH.md)
    - Each match line is annotated with one of the three categories (`# EDIT`, `# READ-ONLY`, `# UNRELATED`)
    - If new EDIT-class matches appear beyond D-25's 4 lines, the task halts and the plan is re-reviewed (acceptance fails loudly, not silently)
    - `bash scripts/red-team-hybrid/check_protected_paths.sh` rc=0 (artifacts/ is allowed)
  </acceptance_criteria>
  <done>Wider sweep captured, classified, committed as audit evidence; D-25 scope verified no-larger-than-claimed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add dated caveats to 05-holdout-cohort-design.md:44 and 06-tier-2-recall-eval.md:74</name>
  <files>
    docs/plans/2026-04-06-red-team-hybrid/05-holdout-cohort-design.md
    docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md
  </files>
  <read_first>
    - docs/plans/2026-04-06-red-team-hybrid/05-holdout-cohort-design.md (lines 30-55 to see the paragraph containing line 44)
    - docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md (lines 65-85 to see the metric table row at line 74)
    - .planning/phases/01-move-0-prep-liveness-prep/1-CONTEXT.md (D-14 for the dated-caveat pattern; D-25 for the 4 targets)
    - .planning/phases/01-move-0-prep-liveness-prep/1-RESEARCH.md §"GOV-01 (withdraw '9% pipeline precision' claim)" (the literal caveat skeleton)
  </read_first>
  <action>
    **Edit 1: `docs/plans/2026-04-06-red-team-hybrid/05-holdout-cohort-design.md`**

    The current paragraph (lines 42-46) reads (verified 2026-04-08):
    ```
    to classify individual signals. The 9% precision metric (per LOB.txt
    evaluation) is on signals; the metric the strategy needs to move is on
    companies that turn into meetings/fundings.
    ```

    Replace this paragraph with:
    ```markdown
    to classify individual signals. ~~The 9% precision metric (per LOB.txt
    evaluation) is on signals; the metric the strategy needs to move is on
    companies that turn into meetings/fundings.~~

    > **Withdrawn 2026-04-08 (GOV-01):** The "9% precision" claim is selection
    > bias per the 2026-04-06 bias audit (`docs/plans/2026-04-06-lob-progress-eval/bias-audit.md`).
    > The 211 labeled signals are an opportunistic sample of suspected FPs, not
    > a random sample. Actual pipeline precision is unknown. The metric the
    > strategy needs to move remains "companies that turn into meetings/fundings"
    > — that part of the original claim is preserved.
    ```

    Preserve the `~~strikethrough~~` of the original sentence so git history + the rendered Markdown both show the withdrawal without corrupting the historical claim. Do NOT delete the original text. Do NOT modify the paragraph's leading sentence `to classify individual signals.` — that sentence stands; only the specific "9% precision metric ..." phrase is struck.

    **Edit 2: `docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md` line ~74**

    The current table row (line 74) reads:
    ```
    | Precision-on-pushed | TPs / (TPs + FPs) on the train cohort | Sanity check against existing ~9% number |
    ```

    Replace with:
    ```markdown
    | Precision-on-pushed | TPs / (TPs + FPs) on the train cohort | ~~Sanity check against existing ~9% number~~ **Withdrawn 2026-04-08 (GOV-01):** 9% is selection bias per the 2026-04-06 bias audit; use precision-on-pushed as a directional indicator only, not as a baseline comparison. See `docs/plans/2026-04-06-lob-progress-eval/bias-audit.md`. |
    ```

    This preserves the metric row itself (the metric is still valid), strikes only the "sanity check against 9%" fragment, and inserts the bias-audit pointer inline in the table cell.

    DO NOT edit any other row in the table. DO NOT reformat the table structure.
  </action>
  <verify>
    <automated>grep "Withdrawn 2026-04-08 (GOV-01)" docs/plans/2026-04-06-red-team-hybrid/05-holdout-cohort-design.md docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Withdrawn 2026-04-08 (GOV-01)" docs/plans/2026-04-06-red-team-hybrid/05-holdout-cohort-design.md` returns exactly `1`
    - `grep -c "Withdrawn 2026-04-08 (GOV-01)" docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md` returns at least `1` (more after Task 3's §11 rewrite)
    - `grep -c "~~The 9% precision metric" docs/plans/2026-04-06-red-team-hybrid/05-holdout-cohort-design.md` returns exactly `1` (strikethrough preserved, not deleted)
    - `grep -c "~~Sanity check against existing ~9% number~~" docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md` returns exactly `1`
    - `grep "bias-audit.md" docs/plans/2026-04-06-red-team-hybrid/05-holdout-cohort-design.md` returns at least one match
    - `grep "bias-audit.md" docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md` returns at least one match
    - Combined diff for this task is ≤30 added lines (well within D-16's ≤50 budget)
    - `bash scripts/red-team-hybrid/check_protected_paths.sh` rc=0
  </acceptance_criteria>
  <done>Targets 1 and 2 caveated with dated strikethrough + bias-audit pointer; originals preserved via strikethrough.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Rewrite §11 of 06-tier-2-recall-eval.md per D-25</name>
  <files>docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md</files>
  <read_first>
    - docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md (lines 260-300 to see the current §11 content that must be replaced)
    - .planning/phases/01-move-0-prep-liveness-prep/1-CONTEXT.md (D-25 explicit REWRITE requirement)
    - .planning/phases/01-move-0-prep-liveness-prep/1-RESEARCH.md §"GOV-01 → For §11 of 06-tier-2-recall-eval.md REWRITE per D-25" (rewrite guidance)
    - docs/plans/2026-04-06-lob-progress-eval/bias-audit.md (source of truth for WHY 9% is selection bias — cite specifically)
  </read_first>
  <action>
    Open `docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md`. Locate §11 (`## 11. The relationship to the existing 9% precision number`) at line 265. The section extends to ~line 285, ending just before `## 12. Open questions` at line 288.

    **Replace the entire §11 section (from `## 11. The relationship to the existing 9% precision number` through the blank line before `## 12. Open questions`) with this new section:**

    ```markdown
    ## 11. Why "9% precision" is not a valid baseline

    **Withdrawn 2026-04-08 (GOV-01).** The previous version of this section framed
    Tier-2 recall against the "9% precision" number from the LOB.txt (Phase 0)
    evaluation. Per the 2026-04-06 bias audit
    (`docs/plans/2026-04-06-lob-progress-eval/bias-audit.md`), the 9% number is
    **selection bias**, not a real pipeline metric:

    - The 211 labeled signals that produced the 9% number were an **opportunistic
      sample of suspected false positives** selected for investigation by the
      analyst. They are NOT a random sample of what the pipeline pushes.
    - Opportunistic samples of suspected FPs overweight the FP tail by
      construction. Any precision number measured on such a sample tells you
      about the sample, not about the pipeline.
    - The bias audit withdraws the claim as a pipeline-level metric. Actual
      pipeline precision is **unknown** until a random-sampled labelling sprint
      runs — which is a Phase 3+ deliverable per `.planning/ROADMAP.md`, not
      Phase 1.

    ### Tier-2 recall is the primary substrate-quality metric

    Going forward, **Tier-2 recall over the hold-out cohort is the primary
    substrate-quality metric for this strategy**, not a derivative of or
    comparison against the 9% number. Specifically:

    - Tier-2 recall is computed on the hold-out split (per §3 of this doc)
    - The metric is measured once per move at the gating contract checkpoint
      (per §4) — never iterated on during a move (per §5)
    - Wilson confidence intervals are mandatory for every Tier-2 recall report
      (per §9) so that small-sample noise is not confused with signal
    - The "do not iterate" rule is especially important because any temptation
      to tune against the Tier-2 number is exactly the same failure mode that
      produced the original selection-biased 9% claim

    ### What the precision/recall dichotomy still tells us

    The pre-withdrawal text correctly noted that precision and recall measure
    different things and move via different levers. That observation is
    preserved:

    - **Precision** improves when classification gets better (Tier-1 work,
      prompt tuning) — and its actual value is currently **unknown**, not 9%
    - **Recall** improves when the substrate captures more, dedups better,
      holds fewer false negatives, and the labelling cohort gets bigger
      (Tracks B/D/E)

    What changes is that we no longer have a baseline precision number to
    reference. Move 1 Tier-2 recall is the FIRST honest number this strategy
    produces. Any comparison to "9% precision" as a baseline is categorically
    wrong — they are not comparable, and the 9% number was never valid anyway.

    See the bias audit for the full reasoning:
    `docs/plans/2026-04-06-lob-progress-eval/bias-audit.md`.

    ```

    Preserve the blank line before `## 12. Open questions`. Do NOT modify §10 or §12. Do NOT modify the Wilson CI requirements in §9.

    After this edit, `## 12. Open questions` should still exist at roughly the same line number (±30 lines for the section-size change).
  </action>
  <verify>
    <automated>grep -c "## 11. Why \"9% precision\" is not a valid baseline" docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## 11. Why \"9% precision\" is not a valid baseline" docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md` returns exactly `1`
    - `grep -c "## 11. The relationship to the existing 9% precision number" docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md` returns exactly `0` (old heading removed)
    - `grep "Tier-2 recall is the primary substrate-quality metric" docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md` returns at least one match
    - `grep "selection bias" docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md` returns at least one match
    - `grep -c "## 12. Open questions" docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md` returns exactly `1` (§12 still exists unchanged)
    - `grep -c "## 10" docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md` returns at least `1` (§10 unchanged)
    - Combined diff for Task 2 + Task 3 is ≤120 added lines (the §11 rewrite is the only large edit; well below any reasonable single-commit budget — note: this commit may exceed the 50-line soft target per D-16 but D-25 explicitly calls for a REWRITE which is inherently larger; document in commit message)
    - `bash scripts/red-team-hybrid/check_protected_paths.sh` rc=0
    - **Post-edit sweep:** re-run `grep -rn "9%" docs/plans/2026-04-06-red-team-hybrid/` and verify every match is either inside a `~~strikethrough~~` block or inside a "Withdrawn 2026-04-08 (GOV-01)" caveat or inside the new §11 "Why 9% precision is not a valid baseline" section
  </acceptance_criteria>
  <done>§11 fully rewritten per D-25; Tier-2 recall framed as primary metric; 9% explicitly retired; all adjacent sections intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → git tree | Markdown-only edits to files in `docs/plans/2026-04-06-red-team-hybrid/`. |
| grep sweep → artifacts | `grep -rn` output captured to `artifacts/gov-01/grep_9pct_sweep.txt`. Read-only filesystem scan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | §11 rewrite corrupting adjacent sections | mitigate | Task 3 acceptance criteria explicitly verify §10 and §12 are unchanged via grep before/after. |
| T-03-02 | Repudiation | Deleting the original 9% claim (silencing the historical record) | mitigate | Targets 1 and 2 use `~~strikethrough~~` not deletion; §11 rewrite is explicitly a replacement with a "Withdrawn 2026-04-08" label referencing the bias audit; git history preserves the pre-rewrite state. |
| T-03-03 | Information disclosure | bias-audit.md path exposure | accept | bias-audit.md is already in the repo and referenced from multiple places; no new exposure. |

<data_integrity>
Three file edits, all additive-or-replacement, all verifiable via grep. Git diff is the audit trail. The wider grep sweep is committed as evidence of D-26 compliance.
</data_integrity>
<supply_chain>
No executable code.
</supply_chain>
<protected_paths>
Zero touches to `collectors/`, `workflows/`, `governance/`, `monitoring/`, `connectors/`, `storage/migrations/`.
</protected_paths>
<secrets>
N/A.
</secrets>
<mitigations>
- `check_protected_paths.sh` pre-commit guard
- `grep_9pct_sweep.txt` committed as D-26 audit evidence
- §10/§12 unchanged verified by grep
- Original text preserved via strikethrough (never deletion)
</mitigations>
</threat_model>

<verification>
Hard rubric gate 7 satisfied by this plan:
- `grep -rn "9%" docs/ .planning/ CLAUDE.md` returns only dated/caveat-wrapped matches
- §11 of `06-tier-2-recall-eval.md` is REWRITTEN per D-25

Verification commands:
```bash
# Gate 7 verification
grep -rn "9%" docs/ .planning/ CLAUDE.md | grep -v "Withdrawn 2026-04-08" | grep -v "bias-audit.md" | grep -v "~~" | grep -v "2026-04-06-lob-progress-eval" | grep -v "8[0-9]%\|9[0-9]%\|98\.69\|93\."
# Expected: no matches (every remaining 9% is either withdrawn, bias-audit, strikethrough, or an unrelated percentage)
grep -c "## 11. Why" docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md  # expect: 1
bash scripts/red-team-hybrid/check_protected_paths.sh  # expect: rc=0
test -f artifacts/gov-01/grep_9pct_sweep.txt  # expect: exists
```
</verification>

<success_criteria>
- 4 known locations caveated per D-25
- §11 rewritten per D-25 REWRITE requirement
- Wider sweep captured as audit evidence per D-26
- All commits pass protected-paths gate
- Hard rubric gate 7 verified green
- GOV-01 traced to this plan's commits
</success_criteria>

<output>
After completion, create `.planning/phases/01-move-0-prep-liveness-prep/01-03-SUMMARY.md` capturing:
- Commit SHAs for the three edits (can be one combined commit or three)
- Output of the final verification grep (should show no unwrapped 9% pipeline precision claims)
- Link to `artifacts/gov-01/grep_9pct_sweep.txt`
- Confirmation GOV-01 closed
- Line counts per edit
</output>
</content>
</invoke>
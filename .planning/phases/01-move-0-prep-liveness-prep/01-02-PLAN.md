---
phase: 01-move-0-prep-liveness-prep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/plans/2026-04-06-red-team-hybrid/00-strategy.md
autonomous: true
requirements: [GOV-04]
tags: [docs, strategy, framing-correction, governance]

must_haves:
  truths:
    - "A dated 'Framing Correction (2026-04-08)' callout exists at the top of 00-strategy.md before §1 'Naming correction'"
    - "The callout explicitly states that substrate work (Track A) and engagement work (LIV-04..LIV-14) are COMPLEMENTARY, NOT SUBSTITUTIVE"
    - "The callout references Move 0.5 as a hard prerequisite to Move 1"
    - "The original §2 'Goal framing (honest version)' text at lines 64-89 is PRESERVED VERBATIM per D-13 (git diff shows zero deletions in that section)"
    - "A reviewer opening 00-strategy.md on 2026-04-18 sees the framing correction at the top before any substrate-specific language"
  artifacts:
    - path: "docs/plans/2026-04-06-red-team-hybrid/00-strategy.md"
      provides: "Framing correction callout prepended before §1"
      contains: "Framing Correction (2026-04-08)"
  key_links:
    - from: "docs/plans/2026-04-06-red-team-hybrid/00-strategy.md"
      to: ".planning/REQUIREMENTS.md (LIV category)"
      via: "callout text references LIV-04..LIV-14 engagement plumbing scope"
      pattern: "LIV-04"
    - from: "docs/plans/2026-04-06-red-team-hybrid/00-strategy.md"
      to: ".planning/ROADMAP.md Phase 2"
      via: "callout text references ROADMAP Phase 2 for engagement plumbing"
      pattern: "ROADMAP.md Phase 2"
---

<objective>
Land the 2026-04-08 framing correction at the top of `00-strategy.md`. The red-team-hybrid strategy doc currently frames substrate hardening as the load-bearing work; the 2026-04-08 synthesis proved that the binding constraint is analyst engagement (evidenced by R19's 38-day silent freeze). Without this correction, the 2026-04-18 regret reviewer (and any future reader) will have a false picture of what the engine is optimizing for.

Purpose: Preserve the historical record of what the 2026-04-06 red-team review concluded while adding a dated delta for the 2026-04-08 synthesis finding. Git history shows both. Reader sees both without confusion.

Output: One modified file (`00-strategy.md`) with a ~6-10 line dated callout prepended before `## 1. Naming correction`. Zero edits to §1-§7 content per D-13.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-move-0-prep-liveness-prep/1-CONTEXT.md
@.planning/phases/01-move-0-prep-liveness-prep/1-RESEARCH.md
@docs/plans/2026-04-06-red-team-hybrid/00-strategy.md

<interfaces>
<!-- Section anchors in 00-strategy.md verified 2026-04-08 per RESEARCH.md -->

```
Line 1:  # Strategy: Direction-A-Derived Hybrid (Substrate Hardening + Parallel Recall Tracks)
Line 2:
Line 3:  **Date:** 2026-04-06
Line 4:  **Owner:** TBD (lead engineer)
Line 5:  **Status:** Move 0 — prep on `prep/red-team-hybrid-prep` branch
Line 6:  **Supersedes:** "Hybrid Engine-Efficacy Strategy" working draft (renamed per red-team §10.2)
Line 7:
Line 8:  ---
Line 9:
Line 10: ## 1. Naming correction
...
Line 64: ## 2. Goal framing (honest version)
Line 91: ## 3. Showstopper guards
Line 133: ## 4. Move sequence
Line 188: ## 5. Decision gates
Line 228: ## 6. Open questions resolved
Line 240: ## 7. What this strategy is NOT
```

The callout inserts BETWEEN line 8 (`---`) and line 10 (`## 1. Naming correction`), adding a new `---` separator after itself.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Prepend Framing Correction callout before §1 of 00-strategy.md</name>
  <files>docs/plans/2026-04-06-red-team-hybrid/00-strategy.md</files>
  <read_first>
    - docs/plans/2026-04-06-red-team-hybrid/00-strategy.md (lines 1-30 to confirm section anchors; lines 64-89 to confirm §2 content that must be preserved verbatim)
    - .planning/phases/01-move-0-prep-liveness-prep/1-CONTEXT.md (D-12, D-13, D-14 for callout requirements)
    - .planning/phases/01-move-0-prep-liveness-prep/1-RESEARCH.md §"GOV-04 framing-correction callout skeleton" (the literal callout text)
    - .planning/REQUIREMENTS.md (LIV category + GOV-04 entry, to verify the external references in the callout are valid)
  </read_first>
  <action>
    Open `docs/plans/2026-04-06-red-team-hybrid/00-strategy.md`. Locate line 10 (`## 1. Naming correction`). Insert the following block IMMEDIATELY BEFORE `## 1. Naming correction`, preserving the blank line above it. The horizontal-rule at the end of the callout is REQUIRED so §1 stays visually distinct.

    Insert this exact text (copy verbatim):

    ```markdown
    > **Framing Correction (2026-04-08):** The 2026-04-06 framing of this strategy
    > as "Track A is substrate hardening, not engine-efficacy improvement" is correct
    > but **incomplete**. The 2026-04-08 5-agent jarvis evaluation + R19 finding
    > proved that the binding constraint is **analyst engagement**, not substrate
    > quality. Substrate work (Tracks A/C/D — this strategy) and engagement work
    > (LIV-04..LIV-14, shipped in Move 0.5) are **complementary, not substitutive**.
    > Either alone fails. Move 0.5 (Liveness Restoration) is now a **hard
    > prerequisite to Move 1**, inserted between Move 0 (this prep window) and the
    > original Move 1. See `.planning/REQUIREMENTS.md` LIV category and
    > `.planning/ROADMAP.md` Phase 2 for the engagement plumbing scope. The §2
    > "honest version" goal framing below is preserved verbatim — this callout
    > annotates it; it does not replace it (per CONTEXT.md D-13).

    ---

    ```

    DO NOT modify §1, §2, §3, §4, §5, §6, §7, or any other content. Per D-13, §2 lines 64-89 must be BYTE-FOR-BYTE IDENTICAL to their pre-edit state. The only change is the callout + horizontal rule prepended before `## 1. Naming correction`.

    After the edit, the file's structure should be:
    ```
    # Strategy: ...
    **Date:** 2026-04-06
    ...
    **Supersedes:** ...

    ---

    > **Framing Correction (2026-04-08):** ... (12 lines of callout) ...
    > annotates it; it does not replace it (per CONTEXT.md D-13).

    ---

    ## 1. Naming correction
    (existing content unchanged)
    ```
  </action>
  <verify>
    <automated>grep -c "Framing Correction (2026-04-08)" docs/plans/2026-04-06-red-team-hybrid/00-strategy.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Framing Correction (2026-04-08)" docs/plans/2026-04-06-red-team-hybrid/00-strategy.md` returns exactly `1`
    - `grep -n "^## 1. Naming correction" docs/plans/2026-04-06-red-team-hybrid/00-strategy.md` shows the heading at line ≥22 (shifted down by ~14 lines from original line 10)
    - `grep "complementary, not substitutive" docs/plans/2026-04-06-red-team-hybrid/00-strategy.md` returns at least one match
    - `grep "hard" docs/plans/2026-04-06-red-team-hybrid/00-strategy.md | grep -c "prerequisite to Move 1"` returns at least `1`
    - `grep "LIV-04..LIV-14" docs/plans/2026-04-06-red-team-hybrid/00-strategy.md` returns at least one match
    - `sed -n '/## 2. Goal framing/,/## 3. Showstopper guards/p' docs/plans/2026-04-06-red-team-hybrid/00-strategy.md` — every line in §2 is byte-for-byte identical to the pre-edit state (verify by comparing to the file at HEAD~1 via `git show HEAD:docs/plans/2026-04-06-red-team-hybrid/00-strategy.md | sed -n '/## 2. Goal framing/,/## 3. Showstopper guards/p'`)
    - Total added-line count in git diff is between 13 and 20 lines (callout + separator + blank lines); satisfies D-16's ≤50 lines per commit
    - `bash scripts/red-team-hybrid/check_protected_paths.sh` exits rc=0 after staging
  </acceptance_criteria>
  <done>Framing correction callout present before §1; §2 preserved verbatim; commit ≤50 lines; protected-paths gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → git tree | Markdown-only edit to a file in `docs/plans/2026-04-06-red-team-hybrid/`. |
| Commit → protected-paths guard | `docs/` is allowed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | §2 verbatim-preservation | mitigate | Edit is strictly additive (prepend). `git diff HEAD~1 -- 00-strategy.md` must show zero deletions inside §2. |
| T-02-02 | Repudiation | Historical record corruption | mitigate | Dated callout ("2026-04-08") preserves the provenance; §2 (the 2026-04-06 framing) is preserved verbatim per D-13 so the historical record is intact. |

<data_integrity>
N/A for DB. File integrity: additive-only edit, git diff reviewable, §2 preservation verifiable via `sed` range extract against HEAD~1.
</data_integrity>
<supply_chain>
No executable code. Pure Markdown.
</supply_chain>
<protected_paths>
Zero touches to `collectors/`, `workflows/`, `governance/`, `monitoring/`, `connectors/`, `storage/migrations/`.
</protected_paths>
<secrets>
N/A.
</secrets>
<mitigations>
- `check_protected_paths.sh` pre-commit guard
- §2 preservation verified by git diff range comparison
- Atomic single-commit per D-16
</mitigations>
</threat_model>

<verification>
Hard rubric gate 6 satisfied by this plan:
- `00-strategy.md` has dated "Framing Correction (2026-04-08)" callout per D-12 (GOV-04)

Verification commands:
```bash
grep -c "Framing Correction (2026-04-08)" docs/plans/2026-04-06-red-team-hybrid/00-strategy.md  # expect: 1
grep "complementary, not substitutive" docs/plans/2026-04-06-red-team-hybrid/00-strategy.md  # expect: present
bash scripts/red-team-hybrid/check_protected_paths.sh  # expect: rc=0
git diff HEAD~1 -- docs/plans/2026-04-06-red-team-hybrid/00-strategy.md | grep "^-" | grep -v "^---" | wc -l  # expect: 0 deletions (pure prepend)
```
</verification>

<success_criteria>
- Framing correction callout landed verbatim per D-12
- §1-§7 content preserved unchanged (zero deletions)
- Commit ≤50 lines
- GOV-04 traced to this commit
- Hard rubric gate 6 verified green
</success_criteria>

<output>
After completion, create `.planning/phases/01-move-0-prep-liveness-prep/01-02-SUMMARY.md` capturing:
- Commit SHA
- Before/after line numbers of `## 1. Naming correction` heading (expected shift: line 10 → ~line 24)
- git diff stat (lines added, lines deleted — deleted should be 0)
- Output of `grep -c "Framing Correction (2026-04-08)"`
- Confirmation GOV-04 closed
</output>
</content>
</invoke>
---
plan: 01-09
phase: 01-move-0-prep-liveness-prep
status: complete
requirements: [REC-04]
commits: [5e881e3, "+1 follow-up data commit"]
---

# Plan 01-09 Summary — Track E founder watchlist seed (REC-04)

## Closes

- **REC-04** — Track E founder watchlist seed via per-collector raw_data extraction

## What shipped

1. **`scripts/red-team-hybrid/extract_founder_candidates.py`** (314 lines, stdlib only)
   - Three per-collector handlers per CONTEXT.md D-27:
     - `extract_arxiv()`: parses `authors[]` from raw_data JSON, filters to papers with company-pattern tokens (Inc, Labs, AI, Corp, Studio, ...) in title/abstract
     - `extract_hacker_news()`: parses `story_text` for self-intro patterns (`I'm <Name>`, `I am <Name>`, `<Name> here, founder of`) since `author` field is HN handle
     - `extract_news_api()`: capitalized phrases near founder/CEO/co-founder
   - `rss_feeds`: SKIPPED per D-27 (low-signal raw_data)
   - sqlite3 read-only URI mode (`file:?mode=ro`)
   - `claude_NNN` founder_id prefix per RESEARCH.md
   - **D-11 hard constraint enforced**: `linkedin_url` is hardcoded to empty string with explicit comment

2. **`scripts/data/founder_watchlist_manual_seed.csv`** (45 lines: 1 header + 44 rows)
   - 44 Claude-extracted founder names merged into the existing populator seed file
   - Schema preserved: `founder_id,full_name,github_username,linkedin_url,associated_company_id`
   - All `linkedin_url` cells empty per D-11

3. **`data/shadow/founder_watchlist.csv`** (45 lines: 1 header + 44 rows)
   - Regenerated by `scripts/build_founder_watchlist.py` (existing populator, untouched)
   - Populator's 7-column schema: `founder_id,full_name,github_username,linkedin_url,source,associated_company_id,added_at`
   - All 44 rows tagged `source=manual_seed`

4. **`data/shadow/founder_reputation_stub.csv`** (10 lines)
   - V2-05 placeholder per D-10 PARALLEL FILE pattern (NOT a column edit to existing populator)
   - Schema: `founder_id,reputation_score,methodology_note`
   - 1 PLACEHOLDER row + methodology comment block

5. **`artifacts/rec-04/founder_candidates_raw.csv`** (51 lines)
   - Extractor buffer file with `source_collector` and `extraction_evidence` traceability columns

## Extractor JSON output

```json
{
  "out": "artifacts\\rec-04\\founder_candidates_raw.csv",
  "total_rows": 44,
  "arxiv": 40,
  "hacker_news": 4,
  "news_api": 0,
  "deduped": 0
}
```

Per RESEARCH.md, news_api was expected to yield 1-3 (only 17 rows in corpus, only 1 with "founder" mention). Actual: 0 — the regex didn't match. Acceptable per D-28 (per-handler heuristic tuning is a soft target). The 44-row total comfortably exceeds the soft rubric gate 9 minimum of 30.

## Acceptance criteria

| Check | Expected | Actual |
|-------|----------|--------|
| `test -f extract_founder_candidates.py` | exit 0 | exit 0 |
| `python -c "import ast; ast.parse(...)"` | exit 0 | exit 0 |
| `wc -l extract_founder_candidates.py` | ≥150 | 314 |
| `def extract_arxiv` | 1 | 1 |
| `def extract_hacker_news` | 1 | 1 |
| `def extract_news_api` | 1 | 1 |
| `def extract_rss_feeds` | 0 | 0 (skipped per D-27) |
| `linkedin_url` references in script | ≥2 | 4 (column ref + hard-constraint comment + dict key + zero-out) |
| `D-11` reference in script | ≥1 | 1 |
| `claude_` prefix references | ≥1 | 1 |
| `mode=ro` references | ≥1 | 1 |
| `REC-04` reference | ≥1 | 1 |
| `test -f founder_watchlist_manual_seed.csv` | exit 0 | exit 0 |
| `grep -c "^claude_" founder_watchlist_manual_seed.csv` | ≥15 | 44 |
| `test -f founder_watchlist.csv` | exit 0 | exit 0 |
| `grep -c "^claude_" founder_watchlist.csv` | ≥15 | 44 |
| populator schema preserved | yes | yes (7-column header) |
| `,manual_seed,` count | ≥15 | 44 |
| **`grep -E ",https?://[^,]*linkedin" *.csv`** | **0** | **0** (D-11) |
| `test -f founder_reputation_stub.csv` | exit 0 | exit 0 |
| reputation stub header | 1 match | 1 |
| `V2-05` count in stub | ≥2 | 4 |
| `git status \| grep build_founder_watchlist.py` | 0 | 0 (populator untouched) |
| `bash check_protected_paths.sh` | rc=0 | rc=0 |

## Soft rubric gate 9 (founder_watchlist ≥30 rows): **GREEN** at 44 rows

## Commits

- `5e881e3` — `feat(01-09): extract_founder_candidates.py for REC-04 Track E seed`
- `+1 data commit` — `feat(01-09): Track E founder watchlist seed data (REC-04, 44 rows)`

## D-11 LinkedIn enforcement evidence

```
$ grep -E ",https?://[^,]*linkedin" data/shadow/founder_watchlist.csv | wc -l
0
$ grep -E ",https?://[^,]*linkedin" scripts/data/founder_watchlist_manual_seed.csv | wc -l
0
$ grep -E ",https?://[^,]*linkedin" artifacts/rec-04/founder_candidates_raw.csv | wc -l
0
$ grep -E ",https?://[^,]*linkedin" data/shadow/founder_reputation_stub.csv | wc -l
0
```

ZERO LinkedIn URLs across all 4 files. The extractor's `assign_founder_ids()` function hardcodes `"linkedin_url": ""` with explicit `# HARD CONSTRAINT D-11: never populated` comment.

## Populator unmodified evidence

```
$ git status --porcelain | grep -c "scripts/build_founder_watchlist.py"
0
```

The existing `scripts/build_founder_watchlist.py` was NOT touched. The reputation stub ships as a PARALLEL file (`data/shadow/founder_reputation_stub.csv`) per D-10's minimal-invasiveness pattern, not as a column edit to the populator's output schema.

## Execution note

The original gsd-executor agent (agent-a9185a82) hit the same compounding failures as 01-08: (a) Bash denied, (b) worktree based off `db0f3bd` (main lineage) instead of expected base `059a18816...`, missing all Phase 1 prerequisites including the populator script, the seed file, and `signals.db`.

Recovery: orchestrator wrote the extractor inline (314 lines, byte-for-byte from plan template lines 124-437 with leading 4-space indent removed), ran it against the `signals.db.pre-step4b-promotion-20260404` backup (since live DB is truncated — see plan 01-08 SUMMARY for the incident report), inline-merged the extractor output into the populator seed file via a python -c heredoc, ran the existing populator with `PYTHONPATH=.` to regenerate the watchlist, and shipped the V2-05 reputation stub via Write tool. All committed atomically.

The empty `worktree-agent-a9185a82` is removed in wave cleanup.

## Deviations from plan

1. **DB source**: ran extractor against `signals.db.pre-step4b-promotion-20260404` instead of live `signals.db` (which is truncated to 4 signals — see 01-08 SUMMARY for the incident report).
2. **Populator invocation**: required `PYTHONPATH=.` because `python scripts/build_founder_watchlist.py` could not import `analytics.shadow_sidecar` from CWD. The plan said to use `python -m scripts.build_founder_watchlist --verbose`, but `python -m` would have required a `__init__.py` in `scripts/` which doesn't exist. `PYTHONPATH=. python scripts/build_founder_watchlist.py` is the equivalent invocation. The populator does not accept a `--verbose` flag; it always prints the row count to stdout.
3. **news_api handler yielded 0 rows** (vs RESEARCH.md estimate of 1-3). The capitalized-phrase-near-founder regex did not match any of the 14 news_api signals' titles or descriptions in the backup DB. Acceptable per D-28 — heuristics are weak by design and the 40 arxiv + 4 HN total of 44 comfortably exceeds the soft rubric gate 9 minimum of 30.
4. **No worktree branch**: applied directly in main worktree because the worktree-based execution path was non-functional. Acceptable per the orchestrator escalation pattern used for plans 01-01, 01-04, 01-07, 01-08.

---
phase: 01-move-0-prep-liveness-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md
autonomous: true
requirements: [LIV-11, GOV-02]
tags: [docs, risk-register, governance, r20]

must_haves:
  truths:
    - "R20 (analyst abandonment) appears in the 'New risks identified during Move 0' table with severity 5 × likelihood 5 = score 25 Showstopper"
    - "R20 row explicitly lists three Phase 1 interim mitigations per D-29: automated keep-alive, daily watchdog alert, STATE.md progress tick"
    - "R20 row explicitly names Phase 2 (Move 0.5) permanent mitigation: LIV-04..LIV-14"
    - "R20 status string reads exactly 'MITIGATING (interim) / Pending permanent (Phase 2)' per D-30"
    - "R20 row also appears in the 'Showstopper status check' table (lines 71-76 area) with partial-verification status"
    - "A reviewer opening 10-risk-register.md on 2026-04-18 can see R20 exists with interim mitigations and know Phase 1 addressed it"
  artifacts:
    - path: "docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md"
      provides: "R20 risk row + showstopper-status R20 row"
      contains: "R20"
  key_links:
    - from: "docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md"
      to: "scripts/red-team-hybrid/install_keepalive_task.ps1"
      via: "mitigation text references the keep-alive installer path"
      pattern: "install_keepalive_task.ps1"
    - from: "docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md"
      to: ".planning/REQUIREMENTS.md (LIV-04..LIV-14)"
      via: "permanent mitigation text references LIV-04..LIV-14"
      pattern: "LIV-04"
---

<objective>
Land R20 (Analyst abandonment — Showstopper score 25) in the red-team-hybrid risk register as a single atomic commit. This closes LIV-11 and GOV-02 in one edit. The R20 row is the governance paper trail the 2026-04-18 Step 4B regret reviewer needs to see in order to know Phase 1 took the new binding-constraint finding seriously.

Purpose: Without R20 captured with explicit Phase 1 interim mitigations per D-29/D-30, the 2026-04-18 regret reviewer has no evidence that the 2026-04-08 synthesis finding (engagement is the binding constraint, not substrate quality) has been operationalized.

Output: One modified file (`10-risk-register.md`) with one commit of ≤50 lines per D-16, containing the R20 row + the Showstopper-status R20 row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-move-0-prep-liveness-prep/1-CONTEXT.md
@.planning/phases/01-move-0-prep-liveness-prep/1-RESEARCH.md
@docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md

<interfaces>
<!-- R19 row at line 53 of 10-risk-register.md is the format template. R20 follows the same 8-column Markdown table schema. -->

From docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md (lines 47-53, verified 2026-04-08):
```
| # | Risk | Sev | Lik | Score | Class | Mitigation | Status |
|---|---|---|---|---|---|---|---|
| **R15** | ... | 4 | 3 | 12 | High Priority | ... | **MONITORING** — ... |
| **R16** | ... | 3 | 3 | 9 | Monitor | ... | **MONITORING** — ... |
| **R17** | ... | 3 | 3 | 9 | Monitor | ... | **MITIGATING** — ... |
| **R18** | ... | 2 | 3 | 6 | Monitor | ... | **DEFERRED** — ... |
| **R19** | **Data collection pipeline silently frozen since 2026-03-01...** | 4 | 5 | **20** | **Showstopper** | Discovered 2026-04-08 by 5-agent codebase audit. ... | **OPEN — REQUIRES DECISION** |
```

R19 is immediately followed by `---` on line 54 (section separator). The R20 row inserts BETWEEN line 53 (R19) and line 54 (the separator).

Showstopper status check table (lines 69-77, verified 2026-04-08):
```
## Showstopper status check

| # | Showstopper | Mitigation in place? | Mitigation verified? |
|---|---|---|---|
| R1 | Step 4B regret window contamination | YES — ... | PARTIAL — ... |
| R2 | Engine-efficacy mechanism gap | YES — ... | TBD — ... |
| **R19** | **Pipeline silently frozen — regret check has no fresh data** | **NO — DISCOVERED 2026-04-08, REQUIRES DECISION** | NO — see R19 row above for the three options |
```

R20 row appends to this second table immediately after the R19 row.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append R20 row to 'New risks identified during Move 0' table</name>
  <files>docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md</files>
  <read_first>
    - docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md (full file — need current R19 row at line 53 as the byte-for-byte format reference)
    - .planning/phases/01-move-0-prep-liveness-prep/1-CONTEXT.md (D-29, D-30 for mitigation content; D-16 for commit size constraint)
    - .planning/phases/01-move-0-prep-liveness-prep/1-RESEARCH.md §"Literal Row Skeletons → R20 row skeleton" (the literal row to paste)
  </read_first>
  <action>
    Open `docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md`. Locate line 53 (the R19 row). Insert the following row IMMEDIATELY AFTER the R19 row and BEFORE the `---` separator on line 54. Use this exact text (copy verbatim):

    ```markdown
    | **R20** | **Analyst abandonment — engagement loop already broken; R19's 38-day silent freeze was empirical proof. Without the analyst opening the inbox the engine has no feedback signal regardless of substrate quality.** | 5 | 5 | **25** | **Showstopper** | **Phase 1 interim mitigations (per CONTEXT.md D-29):** (a) Automated keep-alive — `scripts/red-team-hybrid/install_keepalive_task.ps1` installs Windows Task Scheduler entry running collectors + freshness watchdog daily, removing analyst from the freshness critical path. (b) Daily watchdog alert — keep-alive writes `artifacts/keepalive/YYYY-MM-DD.json` on every run; missing days flag as degraded signal independent of analyst attention. (c) STATE.md progress tick — `.planning/STATE.md` updated at end of each Wave commit so analyst sees movement on days they don't open the repo. **Phase 2 permanent mitigation (Move 0.5 Liveness Restoration, starts 2026-04-19):** LIV-04..LIV-14 — daily digest with empty-channel discipline (LIV-08), calibration positives (LIV-09), `analyst_inbox_engagement_7d` metric (LIV-10), Pandora-lite explanation column (LIV-13), inbox explanation panel (LIV-14), permanent Hold-Review batch (LIV-12). | **MITIGATING (interim) / Pending permanent (Phase 2)** — Phase 1 ships interim per D-29; Phase 2 ships permanent per LIV-04..LIV-14. |
    ```

    DO NOT modify R19 or any other rows. DO NOT reformat the table. DO NOT adjust column widths. The row is a single logical Markdown line (no embedded line breaks).

    Rationale per CONTEXT.md: severity 5 × likelihood 5 = 25 is locked by D-29; the Showstopper class label is locked; the interim mitigation list is locked by D-29 (3 items in order); the Phase 2 mitigation reference is locked by D-30; status string "MITIGATING (interim) / Pending permanent (Phase 2)" is locked by D-30.
  </action>
  <verify>
    <automated>grep -c "^| \*\*R20\*\*" docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^| \*\*R20\*\*" docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md` returns exactly `2` (one in the new-risks table, one in the showstopper-status table added by Task 2)
    - `grep "R20.*Analyst abandonment.*5.*5.*\*\*25\*\*.*Showstopper" docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md` returns at least one match (the severity/score/class triple present)
    - `grep "MITIGATING (interim) / Pending permanent (Phase 2)" docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md` returns exactly one match
    - `grep "install_keepalive_task.ps1" docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md` returns at least one match (keep-alive mitigation referenced)
    - `grep "LIV-04..LIV-14" docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md` returns at least one match (Phase 2 permanent mitigation list referenced)
    - The R19 row at line 53 is UNCHANGED (git diff shows only the new line inserted below it)
    - Total added-line count in git diff is ≤5 lines (one Markdown table row + any table-structure fixes); satisfies D-16's ≤50 lines per commit
  </acceptance_criteria>
  <done>R20 row exists in the 'New risks identified during Move 0' table; R19 unchanged; commit size within budget.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append R20 to the Showstopper status check table</name>
  <files>docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md</files>
  <read_first>
    - docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md (after Task 1's edit; need to see R19 row in the Showstopper status check table at ~line 75)
  </read_first>
  <action>
    Open the same file. Locate the "Showstopper status check" table (heading is `## Showstopper status check`, ~line 69). The existing R19 row in that table is the last row before the summary paragraph that starts with "**R1 and R2 are mitigated in design...**".

    Insert this row IMMEDIATELY AFTER the existing R19 row in the Showstopper status check table:

    ```markdown
    | **R20** | **Analyst abandonment** | YES — Phase 1 ships interim mitigations per CONTEXT.md D-29 (automated keep-alive, daily watchdog alert, STATE.md progress tick); Phase 2 ships permanent mitigation via LIV-04..LIV-14 | PARTIAL — interim mitigation in place; permanent mitigation lands in Phase 2 (Move 0.5 starts 2026-04-19) |
    ```

    Additionally update the trailing summary paragraph. The current line reads:

    > **R1 and R2 are mitigated in design. R19 is open and load-bearing for the 2026-04-18 regret check.** Verification of R1/R2 execution happens at the end of Move 0; R19 requires an explicit decision before then.

    Replace it with:

    > **R1 and R2 are mitigated in design. R19 and R20 are load-bearing for the 2026-04-18 regret check: R19 is resolved by the keep-alive task + LIV-02 freshness watchdog shipped in Phase 1; R20 is mitigated interim in Phase 1 (per CONTEXT.md D-29) and permanently in Phase 2 (LIV-04..LIV-14, Move 0.5).** Verification of R1/R2 execution happens at the end of Move 0; R19/R20 mitigation verification runs at the Phase 1 day-10 hard gate (2026-04-18).

    DO NOT touch any other table row. DO NOT modify R1/R2/R19 rows.
  </action>
  <verify>
    <automated>grep -c "R20" docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "R20" docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md` returns at least `3` (Task 1 row + Task 2 row + updated summary paragraph mention)
    - `grep "R20.*PARTIAL" docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md` returns at least one match (showstopper-status row has PARTIAL verification string)
    - `grep "R19 and R20 are load-bearing" docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md` returns exactly one match (summary paragraph updated)
    - Combined commit for Task 1 + Task 2 is ≤50 modified lines per D-16
    - `bash scripts/red-team-hybrid/check_protected_paths.sh` exits with rc=0 after staging the change (this file lives in `docs/`, which is allowed)
  </acceptance_criteria>
  <done>Showstopper table contains R20 row with PARTIAL status; summary paragraph updated; protected-paths gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → git tree | This plan writes Markdown to a file in `docs/plans/2026-04-06-red-team-hybrid/`. No external inputs, no user-supplied content processed. |
| Commit → protected-paths guard | `check_protected_paths.sh` runs at commit time. `docs/` is in the allowed list; no protected path is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | 10-risk-register.md row insertion | mitigate | Insert is additive (no deletion); R19 row unchanged; git diff reviewable. |
| T-01-02 | Information disclosure | R20 mitigation text | accept | Contents are already internal planning information; no new secrets or PII. |
| T-01-03 | Elevation of privilege | Protected-paths bypass | mitigate | `check_protected_paths.sh` runs pre-commit; `docs/` is allowed; `.claude/hooks/postedit_protected_paths.ps1` catches per-edit escapes. |

<data_integrity>
Read-only signals.db access: N/A (this plan does not touch the DB). File overwrite safety: Edit tool with literal string replacement, no regex/templating; git diff reviewable before commit.
</data_integrity>
<supply_chain>
No new executable code, no installer scripts. Pure Markdown edit.
</supply_chain>
<protected_paths>
Zero touches to `collectors/`, `workflows/`, `governance/`, `monitoring/`, `connectors/`, `storage/migrations/`. Only `docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md` (allowed path) is modified.
</protected_paths>
<secrets>
N/A. No secrets, no API keys, no credentials in scope.
</secrets>
<mitigations>
- `check_protected_paths.sh` pre-commit guard (hard rubric gate 1)
- `.claude/hooks/postedit_protected_paths.ps1` per-edit enforcement
- Atomic single-commit per D-16 (≤50 lines)
- git diff review before commit
</mitigations>
</threat_model>

<verification>
Hard rubric gates satisfied by this plan (from CONTEXT.md):
1. `bash scripts/red-team-hybrid/check_protected_paths.sh` → rc=0 (this plan touches only `docs/`, allowed)
3. `10-risk-register.md` contains R20 row (Severity 5×5=25 Showstopper + interim mitigations per D-30) — **this plan is the direct implementer of gate 3**

Verification commands (run after task completion):
```bash
# Gate 3 verification (hard)
grep -c "^| \*\*R20\*\*" docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md  # expect: 2
grep "R20.*5.*5.*\*\*25\*\*.*Showstopper" docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md  # expect: at least 1
grep "MITIGATING (interim) / Pending permanent (Phase 2)" docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md  # expect: 1
# Gate 1 verification (hard)
bash scripts/red-team-hybrid/check_protected_paths.sh  # expect: rc=0
```
</verification>

<success_criteria>
- R20 row present in both tables of 10-risk-register.md with locked severity/status strings
- Protected-paths gate passes
- Commit size ≤50 lines per D-16
- LIV-11 and GOV-02 both traced to this commit (dual REQ tag in commit message)
- Hard rubric gate 3 verified green
</success_criteria>

<output>
After completion, create `.planning/phases/01-move-0-prep-liveness-prep/01-01-SUMMARY.md` capturing:
- Commit SHA for the R20 row addition
- grep output confirming R20 appears in both tables
- Output of `bash scripts/red-team-hybrid/check_protected_paths.sh` (should be rc=0)
- Line count of the commit (should be ≤50)
- Confirmation that LIV-11 and GOV-02 are both closed
</output>
</content>
</invoke>
---
phase: 01-move-0-prep-liveness-prep
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md
autonomous: true
requirements: [LIV-03, GOV-03]
tags: [docs, governance, preconditions, freshness]

must_haves:
  truths:
    - "A new file `docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` exists"
    - "The file specifies the freshness precondition contract for the 2026-04-18 Step 4B regret check (LIV-03)"
    - "The file specifies the freshness precondition contract for ALL governance gates (GOV-03) — not just Step 4B"
    - "The file includes the verification command (`freshness_watchdog.py --json --threshold-hours 120`), exit-code semantics, blocking-vs-advisory table, escalation path, and Phase 2 implementation hand-off"
    - "The file explicitly links back to R19 (root cause) and R20 (analyst abandonment interim mitigation)"
    - "A reviewer opening this file on 2026-04-18 can run the verification command, interpret the exit code, and decide whether to proceed with or postpone the regret check"
  artifacts:
    - path: "docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md"
      provides: "LIV-03 + GOV-03 contract doc (Phase 1 docs-only spec; Phase 2 implements in code)"
      contains: "freshness precondition"
      min_lines: 90
  key_links:
    - from: "docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md"
      to: "scripts/red-team-hybrid/freshness_watchdog.py"
      via: "verification command invocation"
      pattern: "freshness_watchdog.py"
    - from: "docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md"
      to: "docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md (R19, R20)"
      via: "explicit back-references in §8"
      pattern: "R19.*R20"
---

<objective>
Ship the Phase 1 DOCS-ONLY contract doc that specifies the freshness precondition for the 2026-04-18 Step 4B regret check (LIV-03) and for all governance gates (GOV-03). Per D-20/D-21, `governance/` is a protected path — Phase 1 ships the spec; Phase 2 (after the 2026-04-19 unfreeze) implements the code.

Purpose: The 2026-04-18 regret reviewer needs a single discovery point that states: "here is the freshness precondition, here is how to verify it, here is the blocking-vs-advisory policy, here is the escalation path if freshness fails, here is how this closes R19 and mitigates R20." One coherent document per D-24, two REQs (LIV-03 + GOV-03).

Output: One new file (`14-step4b-preconditions.md`), ~100-150 lines, covering §1-§8 per the RESEARCH.md skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-move-0-prep-liveness-prep/1-CONTEXT.md
@.planning/phases/01-move-0-prep-liveness-prep/1-RESEARCH.md
@scripts/red-team-hybrid/freshness_watchdog.py
@docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md

<interfaces>
<!-- freshness_watchdog.py JSON output shape (verified from scripts/red-team-hybrid/freshness_watchdog.py:~260-290) -->

```json
{
  "checked_at": "2026-04-08T01:35:12.000000+00:00",
  "threshold_hours": 36,
  "exit_code": 0,
  "status": "OK",
  "collectors": [
    {
      "source_api": "hacker_news",
      "max_created_at": "2026-04-08T01:27:54+00:00",
      "age_hours": 0.12,
      "state": "FRESH"
    },
    ...
  ],
  "failures": []
}
```

<!-- freshness_watchdog.py CLI interface -->

```
Usage:
  python scripts/red-team-hybrid/freshness_watchdog.py              # default 36h threshold, text output
  python scripts/red-team-hybrid/freshness_watchdog.py --json       # JSON output for CI/cron
  python scripts/red-team-hybrid/freshness_watchdog.py --threshold-hours 120  # 5-day threshold for governance gates
  python scripts/red-team-hybrid/freshness_watchdog.py --operational hacker_news,arxiv  # override operational set

Exit codes:
  0 = all operational collectors FRESH
  1 = at least one operational collector STALE (gate should fail/postpone)
  2 = operational error (DB unreadable, schema mismatch) — escalate, do NOT silently pass
```

<!-- governance/ package files (protected, do NOT edit in Phase 1) -->

- `governance/cli.py` — governance CLI command surface
- `governance/contracts.py` — contract definitions
- `governance/state_policies.py` — state promotion policies
- `governance/writer.py` — audit_events writer
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 14-step4b-preconditions.md with LIV-03 + GOV-03 contract</name>
  <files>docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md</files>
  <read_first>
    - scripts/red-team-hybrid/freshness_watchdog.py (lines 1-120 — docstring, exit codes, CLI args; lines 260-290 — JSON output shape used by §3 of the new doc)
    - docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md (R19 and R20 rows for §8 cross-reference; R20 may or may not exist depending on plan 01-01 execution order — the doc should reference R20 regardless since plan 01-01 is in the same wave)
    - .planning/phases/01-move-0-prep-liveness-prep/1-CONTEXT.md (D-20 contract requirements, D-24 file location lock, D-29 R20 interim mitigation context)
    - .planning/phases/01-move-0-prep-liveness-prep/1-RESEARCH.md §"14-step4b-preconditions.md skeleton" (the literal section skeleton per D-19/D-20)
    - .planning/REQUIREMENTS.md (LIV-03 and GOV-03 definitions)
  </read_first>
  <action>
    Create a NEW file at `docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md`. Write the following exact content (this is the literal skeleton from RESEARCH.md §"14-step4b-preconditions.md skeleton" expanded to a shippable doc):

    ```markdown
    # Step 4B Preconditions and Governance Gate Freshness Contract

    **Date:** 2026-04-08
    **Status:** Spec — implemented in code in Phase 2 (after 2026-04-19 unfreeze)
    **REQ:** LIV-03 (regret check precondition) + GOV-03 (gate contract for all governance gates)
    **Resolves:** R19 root cause + R20 interim mitigation surface for the governance lane

    ---

    ## 1. The contract

    Every governance gate (Step 4B regret check, canary runs, drift alerts,
    future state-promotion gates) requires `max(detected_at) < 5 days` for
    **at least 3 of the 4 operational collectors** over the prior 7 days,
    BEFORE the gate evaluates its primary condition.

    The 4 operational collectors (locked in `scripts/red-team-hybrid/freshness_watchdog.py`
    `DEFAULT_OPERATIONAL_COLLECTORS`) are:

    - `hacker_news`
    - `arxiv`
    - `rss_feeds`
    - `news_api`

    If fewer than 3 of these 4 collectors are fresh within the 5-day window,
    the governance gate MUST NOT evaluate its primary condition. See §4 for
    blocking vs advisory behavior per gate.

    ---

    ## 2. Verification command

    The freshness precondition is verified by `scripts/red-team-hybrid/freshness_watchdog.py`,
    which is the LIV-02 deliverable already shipped in commit `4efe8cf`:

    ```bash
    python scripts/red-team-hybrid/freshness_watchdog.py --json --threshold-hours 120
    ```

    The `--threshold-hours 120` flag sets the 5-day window (120 hours = 5 days)
    required by the gate contract. The default threshold of 36 hours is the
    operational-monitoring threshold, not the gate-precondition threshold —
    the two thresholds intentionally differ because the gate tolerates more
    latency than the day-to-day freshness alert.

    ### Exit code semantics

    | Exit code | Meaning | Gate action |
    |-----------|---------|-------------|
    | `0` | All 4 operational collectors fresh within the 120h window (≥3 minimum satisfied by 4/4) | **Proceed** — gate evaluates its primary condition |
    | `1` | At least one operational collector stale (check `failures[]` array for which ones) — if fewer than 3 remain fresh, precondition fails | **Postpone** per LIV-03 escalation; see §5 |
    | `2` | Operational error (DB unreadable, schema mismatch, freshness_watchdog.py internal failure) | **Escalate** — do NOT silently pass; this is not a fail-open case |

    Gate evaluators MUST treat exit code `2` as a hard escalation, not a
    fail-open default. A fail-open on operational errors would reintroduce
    R19's silent-failure mode at the gate layer.

    ---

    ## 3. Required precondition input format

    Every gate evaluator MUST capture the freshness watchdog JSON output as
    part of the gate's `audit_events` row. The schema matches
    `scripts/red-team-hybrid/freshness_watchdog.py` `render_json()`:

    ```json
    {
      "checked_at": "<ISO 8601 timestamp>",
      "threshold_hours": 120,
      "exit_code": 0,
      "status": "OK",
      "collectors": [
        {
          "source_api": "hacker_news",
          "max_created_at": "<ISO 8601>",
          "age_hours": 0.12,
          "state": "FRESH"
        }
      ],
      "failures": []
    }
    ```

    The `status` field is one of `OK`, `STALE`, or `ERROR`. The `failures`
    array lists the source_api strings that failed the threshold, empty if
    all pass. Phase 2 implementation stores this blob alongside the gate's
    pass/fail decision so that post-hoc analysis can always see "what freshness
    state was the gate evaluated against".

    ---

    ## 4. Blocking vs advisory behavior

    | Gate | Phase 1 (this doc) | Phase 2 (after unfreeze) |
    |------|--------------------|----------------------------|
    | Step 4B regret check (2026-04-18) | **BLOCKING** — postpones if freshness fails per LIV-03 escalation | **BLOCKING** — same |
    | Daily canary runs | ADVISORY — log warning, do not block | **BLOCKING** after 30 days zero false-positives |
    | SPC drift alerts | ADVISORY — alerts gate on freshness state | **BLOCKING** after Phase 2 ships the precondition-wrapper |
    | Future state-promotion gates | DOCS-ONLY in Phase 1 | **BLOCKING** in Phase 2 |

    The Step 4B regret check is the gate this contract exists for. The other
    gates inherit the contract by reference — the blocking semantics are
    phased in over Phase 2 to avoid breaking existing monitoring cadence
    during the Move 0.5 liveness ship.

    ---

    ## 5. Failure escalation path

    1. Freshness watchdog returns exit code `1` → **automated postpone** of
       the gate (no human action required for the postpone itself).
    2. Postpone event written to `audit_events` with reason
       `freshness_failure`, referencing the watchdog JSON output.
    3. `.planning/STATE.md` updated with the postpone event on the same day
       per D-34's day-by-day gate evaluation requirement.
    4. Next regret check date computed as `now + 5 days` (the freshness
       window — giving enough time for the pipeline to catch up).
    5. If freshness still fails after **3 consecutive postpone cycles**,
       escalate to human review for root-cause investigation. Do NOT
       auto-postpone indefinitely — that would silently convert R19 back
       into a permanent freeze of governance.

    ### Error path (exit code 2)

    1. Freshness watchdog returns exit code `2` → immediate human escalation.
    2. Error detail logged to `artifacts/governance-gate-errors/<date>.json`.
    3. Gate does NOT evaluate; gate does NOT postpone on a timer. It waits
       for human intervention.
    4. Phase 1 has no automation for this path — if exit code 2 fires between
       2026-04-08 and 2026-04-19, the human-in-the-loop path is the only
       remediation until Phase 2 ships better tooling.

    ---

    ## 6. Gates this contract applies to

    - **Step 4B regret check** (governance event #21, due 2026-04-18) —
      THE PRIMARY GATE THIS CONTRACT EXISTS FOR. This is the load-bearing
      use case. Phase 1 ships this contract specifically to prevent the
      2026-04-18 check from evaluating against silently-frozen data.
    - **Daily canary runs** (Phase 2+) — ADVISORY in Phase 1, BLOCKING in
      Phase 2. Canary runs benefit from freshness context even when not
      blocked by it.
    - **SPC drift alerts** (Phase 2+) — ADVISORY in Phase 1, BLOCKING in
      Phase 2 once the precondition-wrapper ships.
    - **Future state-promotion gates** (Phase 2+) — any new gate added after
      2026-04-19 MUST inherit this contract as a BLOCKING precondition by
      default.

    ---

    ## 7. Phase 2 implementation hand-off

    Phase 2 implements this contract in the `governance/` package, which is
    currently a protected path (Move 0 protected-paths list, unfreezes
    2026-04-19). Specifically:

    - **`governance/cli.py`** — adds a `--require-freshness` flag (or equivalent
      subcommand wrapping) that runs the freshness watchdog before evaluating
      any gate. Default: `True` for Step 4B regret check, `False` (advisory)
      for canaries and drift alerts until the 30-day zero-FP soak completes.
    - **`governance/state_policies.py`** — adds a `freshness_precondition`
      field to the policy schema. Default `True` for gates listed in §6;
      explicit `False` required (with audit rationale) to disable.
    - **`audit_events`** schema — gains a `precondition_audit_json` column,
      OR a new `gate_preconditions` child table keyed on `audit_event_id`.
      Decision deferred to Phase 2 day 1 — the schema change touches
      `storage/migrations/` (also protected until 2026-04-19).

    **These code changes are OUT OF SCOPE for Phase 1** per CONTEXT.md D-20
    and D-21. Phase 1 ships THIS DOCUMENT and nothing else in governance.
    Phase 2 ships the code. Any Phase 1 plan proposing an edit to
    `governance/*.py` or `storage/migrations/` is rejected by
    `scripts/red-team-hybrid/check_protected_paths.sh`.

    ---

    ## 8. Link back to R19 and R20

    This contract exists because of R19 and R20 in
    `docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md`:

    - **R19 (frozen pipeline)** — this contract **closes R19** by ensuring
      no governance gate can pass against silently-frozen data. R19 is
      root-caused by "no scheduled collection + no freshness verification
      at gate-time". LIV-01 restarted the pipeline; LIV-02 shipped the
      watchdog; the keep-alive installer (CONTEXT.md D-22) makes collection
      scheduled; THIS CONTRACT adds the gate-time verification.
    - **R20 (analyst abandonment)** — this contract is part of the Phase 1
      **interim mitigation suite** per CONTEXT.md D-29 by removing the
      analyst from the freshness critical path for governance gates. The
      analyst does not have to remember to check freshness before the
      2026-04-18 regret check — the gate itself checks, postpones on
      failure, and logs the decision to `audit_events` + `STATE.md` without
      analyst involvement.

    See `10-risk-register.md` R19 and R20 rows for full context. See
    `.planning/REQUIREMENTS.md` LIV-03 and GOV-03 for the originating REQ
    definitions.

    ---

    *Phase: 01-move-0-prep-liveness-prep*
    *REQ: LIV-03 + GOV-03*
    *Status: Phase 1 docs-only spec; Phase 2 implements the code*
    ```

    The file is ~150 lines of Markdown. It is the single discovery point for the 2026-04-18 reviewer and the Phase 2 implementer. Both LIV-03 (regret check precondition) and GOV-03 (all-gates contract) are satisfied by this one file per D-24.

    **DO NOT** edit any `governance/*.py` file. **DO NOT** edit `storage/migrations/`. The contract is docs-only per D-20.
  </action>
  <verify>
    <automated>test -f docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md && wc -l docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` → exit 0
    - `wc -l docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` reports at least `90` lines
    - `grep -c "^## 1\. The contract" docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` returns `1`
    - `grep -c "^## 2\. Verification command" docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` returns `1`
    - `grep -c "^## 3\. Required precondition input format" docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` returns `1`
    - `grep -c "^## 4\. Blocking vs advisory behavior" docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` returns `1`
    - `grep -c "^## 5\. Failure escalation path" docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` returns `1`
    - `grep -c "^## 6\. Gates this contract applies to" docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` returns `1`
    - `grep -c "^## 7\. Phase 2 implementation hand-off" docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` returns `1`
    - `grep -c "^## 8\. Link back to R19 and R20" docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` returns `1`
    - `grep "freshness_watchdog.py --json --threshold-hours 120" docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` returns at least one match
    - `grep "exit code.*2" docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` returns at least one match (error escalation path specified)
    - `grep "LIV-03" docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` returns at least one match
    - `grep "GOV-03" docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` returns at least one match
    - `grep "R19" docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` returns at least 2 matches
    - `grep "R20" docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` returns at least 2 matches
    - `grep "3 of the 4 operational collectors" docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` returns exactly 1 match (the ≥3 threshold is specified)
    - `bash scripts/red-team-hybrid/check_protected_paths.sh` rc=0
    - Zero `governance/` or `storage/migrations/` files modified (verified by `git status --porcelain | grep -E '^(M|A).*governance/' | wc -l` returning `0`)
  </acceptance_criteria>
  <done>New file exists with all 8 sections, cites LIV-03 and GOV-03, cites R19 and R20, specifies verification command + exit codes + escalation path + Phase 2 hand-off; no protected-path edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → git tree | New Markdown file in `docs/plans/2026-04-06-red-team-hybrid/` (allowed path). |
| Contract → Phase 2 code | This doc is a spec. Phase 2 code implementation is explicitly out-of-scope and must not be attempted in Phase 1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Implicit code edits creeping in | mitigate | Task action explicitly prohibits `governance/*.py` and `storage/migrations/` edits. `check_protected_paths.sh` enforces at commit time. |
| T-04-02 | Denial of service | Fail-open on exit code 2 | mitigate | §5 explicitly specifies exit code 2 as HARD ESCALATE, not fail-open; Phase 2 implementers inherit this from the doc. |
| T-04-03 | Repudiation | Silent indefinite postponement | mitigate | §5 caps auto-postpone at 3 consecutive cycles; beyond that, human review required. |
| T-04-04 | Elevation of privilege | Phase 1 trying to "just quickly add" the governance code | mitigate | D-21 explicit plan-reject rule; `check_protected_paths.sh` hard enforcement. |

<data_integrity>
No DB access. Pure file creation. `freshness_watchdog.py` is referenced but not invoked by this plan.
</data_integrity>
<supply_chain>
No new executable code. Pure Markdown spec.
</supply_chain>
<protected_paths>
Zero touches to `collectors/`, `workflows/`, `governance/`, `monitoring/`, `connectors/`, `storage/migrations/`. The ONLY file modified is `docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` (new file in allowed path).
</protected_paths>
<secrets>
N/A.
</secrets>
<mitigations>
- `check_protected_paths.sh` pre-commit guard
- Explicit governance-path prohibition in task action
- Post-commit grep verifying zero governance/ or storage/migrations/ edits
- Single-file commit keeps diff reviewable
</mitigations>
</threat_model>

<verification>
Hard rubric gate 4 satisfied by this plan:
- `14-step4b-preconditions.md` exists and specifies LIV-03 preconditions + GOV-03 gate contract

Verification commands:
```bash
test -f docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md  # expect: exit 0
grep -c "^## " docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md  # expect: ≥8 (sections 1-8)
grep "LIV-03" docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md  # expect: present
grep "GOV-03" docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md  # expect: present
bash scripts/red-team-hybrid/check_protected_paths.sh  # expect: rc=0
git status --porcelain | grep -E '^(M|A).*governance/' | wc -l  # expect: 0
```
</verification>

<success_criteria>
- New contract doc exists at the D-24 locked location
- All 8 sections present and substantive
- LIV-03 and GOV-03 both traced to this plan's commit
- Zero protected-path edits
- Hard rubric gate 4 verified green
</success_criteria>

<output>
After completion, create `.planning/phases/01-move-0-prep-liveness-prep/01-04-SUMMARY.md` capturing:
- Commit SHA
- `wc -l` output for the new file
- grep summary of section headers
- Confirmation LIV-03 and GOV-03 both closed
- Confirmation zero protected-path edits
</output>
</content>
</invoke>
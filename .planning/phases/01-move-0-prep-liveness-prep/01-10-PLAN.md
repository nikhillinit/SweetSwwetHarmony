---
phase: 01-move-0-prep-liveness-prep
plan: 10
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03, 01-04, 01-05, 01-06, 01-07, 01-08, 01-09]
files_modified:
  - .planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md
  - .planning/STATE.md
autonomous: true
requirements: [SUB-01]
tags: [verification, charter-rollup, sub-01, state]

must_haves:
  truths:
    - "A new file `.planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md` exists with the SUB-01 charter deliverables matrix walked end-to-end"
    - "The verification matrix confirms all 12 charter deliverables (D1-D12) per `01-move-0-charter.md` are at the 80%+ level"
    - "The verification matrix confirms all extra Phase 1 deliverables (R20 row, 14-step4b-preconditions.md, 13-track-d-design.md, Track B/C/E seeds, keep-alive installer) exist"
    - "Every hard rubric gate (1-7) is evaluated with the actual command run and result captured"
    - "Every soft rubric gate (8-13) is evaluated; shortfalls documented per D-03 with carry-over plan to Phase 2"
    - "All 11 Phase 1 REQs (LIV-03, LIV-11, GOV-01, GOV-02, GOV-03, GOV-04, SUB-01, REC-01, REC-02, REC-03, REC-04) are traced to specific commits/files"
    - ".planning/STATE.md is updated with day-by-day gate evaluation entries per D-34"
    - "The Phase 1 → Phase 2 handoff inputs (D-31) are confirmed present with expected schemas"
  artifacts:
    - path: ".planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md"
      provides: "Phase 1 SUB-01 charter rollup + hard/soft rubric gate evaluation + REQ traceability"
      min_lines: 200
    - path: ".planning/STATE.md"
      provides: "Updated with day-by-day gate evaluations per D-34"
      contains: "Phase 1"
  key_links:
    - from: ".planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md"
      to: "docs/plans/2026-04-06-red-team-hybrid/01-move-0-charter.md §3 D1-D12"
      via: "12-row charter deliverable matrix walked"
      pattern: "D1.*D12"
    - from: ".planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md"
      to: "all 9 prior plan SUMMARYs"
      via: "REQ traceability table cites each plan's commit"
      pattern: "01-0[1-9]-SUMMARY"
---

<objective>
Execute the Wave C synthesis: SUB-01 verification of all Move 0 charter deliverables, run all 7 hard rubric gates + 6 soft rubric gates with actual commands, trace all 11 Phase 1 REQs to specific commits, confirm Phase 1 → Phase 2 handoff inputs (D-31) exist, and update STATE.md per D-34. This plan produces the Phase 1 VERIFICATION.md that the 2026-04-18 Step 4B regret reviewer reads.

Per CONTEXT.md D-15 Wave C, this is the rollup that depends on Waves A and B landing. It is the only plan with explicit `depends_on` of all 9 prior plans.

Output:
1. `.planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md` — full verification report
2. Updated `.planning/STATE.md` with Phase 1 gate evaluations
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-move-0-prep-liveness-prep/1-CONTEXT.md
@.planning/phases/01-move-0-prep-liveness-prep/1-RESEARCH.md
@docs/plans/2026-04-06-red-team-hybrid/01-move-0-charter.md
@.planning/phases/01-move-0-prep-liveness-prep/01-01-SUMMARY.md
@.planning/phases/01-move-0-prep-liveness-prep/01-02-SUMMARY.md
@.planning/phases/01-move-0-prep-liveness-prep/01-03-SUMMARY.md
@.planning/phases/01-move-0-prep-liveness-prep/01-04-SUMMARY.md
@.planning/phases/01-move-0-prep-liveness-prep/01-05-SUMMARY.md
@.planning/phases/01-move-0-prep-liveness-prep/01-06-SUMMARY.md
@.planning/phases/01-move-0-prep-liveness-prep/01-07-SUMMARY.md
@.planning/phases/01-move-0-prep-liveness-prep/01-08-SUMMARY.md
@.planning/phases/01-move-0-prep-liveness-prep/01-09-SUMMARY.md

<interfaces>
<!-- Move 0 charter D1-D12 (per 01-move-0-charter.md §3, verified 2026-04-08) -->

| # | Deliverable | Location |
|---|---|---|
| D1 | Strategy charter (renamed) | 00-strategy.md |
| D2 | Move 0 charter | 01-move-0-charter.md |
| D3 | Bounded-context map | 02-bounded-context-map.md |
| D4 | Dead-letter contract | 03-dead-letter-contract.md |
| D5 | LLM failure mode decision | 04-llm-failure-mode.md |
| D6 | Hold-out cohort split design | 05-holdout-cohort-design.md |
| D7 | Tier-2 recall eval design | 06-tier-2-recall-eval.md |
| D8 | Systematic collector audit catalog | 07-collector-audit.md |
| D9 | Track B labelling sprint plan | 08-track-b-labelling.md |
| D10 | Track E founder watchlist | 09-track-e-watchlist.md + populated CSV |
| D11 | Risk register snapshot | 10-risk-register.md |
| D12 | Protected-paths verifier | scripts/red-team-hybrid/check_protected_paths.sh |

<!-- Hard rubric gates (1-7 per CONTEXT.md) -->

1. `bash scripts/red-team-hybrid/check_protected_paths.sh` → rc=0
2. `python scripts/red-team-hybrid/freshness_watchdog.py --json` → rc=0, 4 collectors FRESH
3. R20 row in `10-risk-register.md` (severity 5×5=25 + interim mitigations)
4. `14-step4b-preconditions.md` exists with LIV-03 + GOV-03 contract
5. Keep-alive task installed AND ran ≥2 times (evidence in `artifacts/keepalive/`)
6. `00-strategy.md` has dated "Framing Correction (2026-04-08)" callout
7. `grep -rn "9%" docs/ .planning/ CLAUDE.md` returns only dated/caveat-wrapped matches; §11 of `06-tier-2-recall-eval.md` REWRITTEN

<!-- Soft rubric gates (8-13 per CONTEXT.md) -->

8. `data/shadow/track_b_episodes.csv` ≥15 rows
9. `data/shadow/founder_watchlist.csv` ≥30 rows
10. `data/shadow/holdout_split/*.csv` committed
11. `13-track-d-design.md` exists with 5 D-19 answers
12. `.planning/STATE.md` has day-by-day gate evaluations per D-34
13. Phase 1 → Phase 2 handoff inputs (D-31) exist with expected schemas

<!-- D-31 Phase 1 → Phase 2 handoff inputs -->

1. `scripts/red-team-hybrid/freshness_watchdog.py --json` — freshness for digest header
2. `data/shadow/track_b_episodes.csv` — label progress count
3. `data/shadow/founder_watchlist.csv` — watchlist count
4. `docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` — governance gate contract
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run all 7 hard rubric gates + 6 soft rubric gates, capture results</name>
  <files>artifacts/sub-01/rubric_gate_results.txt</files>
  <read_first>
    - .planning/phases/01-move-0-prep-liveness-prep/1-CONTEXT.md (rubric section — 7 hard + 6 soft gates)
    - .planning/phases/01-move-0-prep-liveness-prep/01-0[1-9]-SUMMARY.md (any prior plan summaries that exist; some may be missing if Phase 1 plans ran out of order — capture which are present)
  </read_first>
  <action>
    Create the `artifacts/sub-01/` directory and capture every rubric gate's actual command output.

    ```bash
    mkdir -p artifacts/sub-01
    OUTPUT=artifacts/sub-01/rubric_gate_results.txt
    : > "$OUTPUT"  # truncate
    ```

    For each gate, run the command and append `=== GATE N === <name> === <RESULT> ===` followed by the actual output. Use this skeleton:

    ```bash
    {
      echo "Phase 1 Rubric Gate Results"
      echo "Generated: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
      echo "Branch: $(git rev-parse --abbrev-ref HEAD)"
      echo "Head SHA: $(git rev-parse --short HEAD)"
      echo ""
      echo "================================================================"
      echo "HARD GATES (1-7) — any fail blocks the phase per LIV-03"
      echo "================================================================"
      echo ""

      echo "=== HARD GATE 1: check_protected_paths.sh ==="
      bash scripts/red-team-hybrid/check_protected_paths.sh
      echo "rc=$?"
      echo ""

      echo "=== HARD GATE 2: freshness_watchdog.py --json ==="
      python scripts/red-team-hybrid/freshness_watchdog.py --json
      echo "rc=$?"
      echo ""

      echo "=== HARD GATE 3: R20 row in 10-risk-register.md ==="
      grep -c "^| \*\*R20\*\*" docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md
      grep "R20.*5.*5.*\*\*25\*\*.*Showstopper" docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md | head -1
      grep "MITIGATING (interim) / Pending permanent (Phase 2)" docs/plans/2026-04-06-red-team-hybrid/10-risk-register.md | head -1
      echo ""

      echo "=== HARD GATE 4: 14-step4b-preconditions.md exists with LIV-03 + GOV-03 ==="
      test -f docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md
      echo "exists=$?"
      grep -c "^## " docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md
      grep -c "LIV-03" docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md
      grep -c "GOV-03" docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md
      echo ""

      echo "=== HARD GATE 5: keep-alive installer + ≥2 runs ==="
      test -f scripts/red-team-hybrid/install_keepalive_task.ps1
      echo "installer_exists=$?"
      test -f artifacts/keepalive/.gitkeep
      echo "evidence_dir_exists=$?"
      ls artifacts/keepalive/*.json 2>/dev/null | wc -l
      echo "json_evidence_files"
      # If there's a deferred install note, capture it
      test -f .planning/phases/01-move-0-prep-liveness-prep/KEEPALIVE-INSTALL-DEFERRED.md && echo "DEFERRED-NOTE-PRESENT" || echo "no-deferred-note"
      echo ""

      echo "=== HARD GATE 6: 00-strategy.md framing correction callout ==="
      grep -c "Framing Correction (2026-04-08)" docs/plans/2026-04-06-red-team-hybrid/00-strategy.md
      grep "complementary, not substitutive" docs/plans/2026-04-06-red-team-hybrid/00-strategy.md | head -1
      echo ""

      echo "=== HARD GATE 7: 9% withdrawal complete ==="
      grep -c "## 11. Why \"9% precision\" is not a valid baseline" docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md
      grep -c "Withdrawn 2026-04-08 (GOV-01)" docs/plans/2026-04-06-red-team-hybrid/05-holdout-cohort-design.md
      grep -c "Withdrawn 2026-04-08 (GOV-01)" docs/plans/2026-04-06-red-team-hybrid/06-tier-2-recall-eval.md
      test -f artifacts/gov-01/grep_9pct_sweep.txt
      echo "sweep_artifact_exists=$?"
      echo ""

      echo "================================================================"
      echo "SOFT GATES (8-13) — fail = documented in VERIFICATION.md, not blocked"
      echo "================================================================"
      echo ""

      echo "=== SOFT GATE 8: track_b_episodes.csv ≥15 rows ==="
      grep -v "^#" data/shadow/track_b_episodes.csv | grep -v "^signal_id" | wc -l
      head -1 data/shadow/track_b_episodes.csv
      echo ""

      echo "=== SOFT GATE 9: founder_watchlist.csv ≥30 rows ==="
      grep -v "^#" data/shadow/founder_watchlist.csv | grep -v "^founder_id" | wc -l
      grep -c "^claude_" data/shadow/founder_watchlist.csv
      grep -E ",https?://[^,]*linkedin" data/shadow/founder_watchlist.csv | wc -l
      echo "linkedin_count"
      echo ""

      echo "=== SOFT GATE 10: holdout_split committed ==="
      ls data/shadow/holdout_split/*.csv 2>/dev/null
      wc -l data/shadow/holdout_split/episodes_v1.csv 2>/dev/null
      echo ""

      echo "=== SOFT GATE 11: 13-track-d-design.md with 5 answers ==="
      test -f docs/plans/2026-04-06-red-team-hybrid/13-track-d-design.md
      echo "exists=$?"
      grep -c "^## [1-5]\." docs/plans/2026-04-06-red-team-hybrid/13-track-d-design.md
      grep -ci "TBD" docs/plans/2026-04-06-red-team-hybrid/13-track-d-design.md
      echo "tbd_count (must be 0)"
      echo ""

      echo "=== SOFT GATE 12: STATE.md day-by-day gates ==="
      grep -c "Phase 1" .planning/STATE.md
      echo ""

      echo "=== SOFT GATE 13: Phase 1 → Phase 2 handoff inputs (D-31) ==="
      python scripts/red-team-hybrid/freshness_watchdog.py --json > /dev/null 2>&1
      echo "freshness_watchdog_runnable=$?"
      test -f data/shadow/track_b_episodes.csv && echo "track_b_csv=present" || echo "track_b_csv=MISSING"
      test -f data/shadow/founder_watchlist.csv && echo "founder_csv=present" || echo "founder_csv=MISSING"
      test -f docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md && echo "preconditions_doc=present" || echo "preconditions_doc=MISSING"
      echo ""

      echo "================================================================"
      echo "REQ TRACEABILITY"
      echo "================================================================"
      for plan in 01-01 01-02 01-03 01-04 01-05 01-06 01-07 01-08 01-09; do
        echo ""
        echo "=== plan $plan ==="
        if test -f .planning/phases/01-move-0-prep-liveness-prep/${plan}-SUMMARY.md; then
          head -20 .planning/phases/01-move-0-prep-liveness-prep/${plan}-SUMMARY.md
        else
          echo "MISSING SUMMARY for plan $plan"
        fi
      done
    } > "$OUTPUT" 2>&1

    cat "$OUTPUT"
    ```

    The output captures the literal command output for every gate. Commit `artifacts/sub-01/rubric_gate_results.txt` as the audit trail.

    For each FAIL or shortfall, note it for inclusion in Task 2's VERIFICATION.md.
  </action>
  <verify>
    <automated>test -f artifacts/sub-01/rubric_gate_results.txt && wc -l artifacts/sub-01/rubric_gate_results.txt</automated>
  </verify>
  <acceptance_criteria>
    - `test -f artifacts/sub-01/rubric_gate_results.txt` → exit 0
    - `wc -l artifacts/sub-01/rubric_gate_results.txt` reports at least `60` lines
    - `grep -c "^=== HARD GATE" artifacts/sub-01/rubric_gate_results.txt` returns at least `7`
    - `grep -c "^=== SOFT GATE" artifacts/sub-01/rubric_gate_results.txt` returns at least `6`
    - `grep -c "REQ TRACEABILITY" artifacts/sub-01/rubric_gate_results.txt` returns at least `1`
    - The output references commands actually run (not placeholders)
    - `bash scripts/red-team-hybrid/check_protected_paths.sh` rc=0
  </acceptance_criteria>
  <done>All 13 rubric gates evaluated, results captured to a single committed audit file.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build VERIFICATION.md with charter matrix + REQ traceability + gate results</name>
  <files>.planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md</files>
  <read_first>
    - artifacts/sub-01/rubric_gate_results.txt (Task 1 output)
    - All `01-0[1-9]-SUMMARY.md` files that exist (paste relevant SHAs/counts)
    - docs/plans/2026-04-06-red-team-hybrid/01-move-0-charter.md (D1-D12 deliverables source of truth)
    - .planning/phases/01-move-0-prep-liveness-prep/1-CONTEXT.md (rubric section to copy verbatim into VERIFICATION.md)
    - .planning/phases/01-move-0-prep-liveness-prep/1-RESEARCH.md §"SUB-01 (Move 0 charter deliverables verification)" (charter cross-reference matrix)
  </read_first>
  <action>
    Create `.planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md` with the following structure. Replace `<...>` placeholders with actual values from Task 1's output and the prior plan SUMMARYs.

    ```markdown
    # Phase 1 Verification — Move 0 Prep + Liveness Prep

    **Date:** <ISO 8601 of run>
    **Branch:** prep/red-team-hybrid-prep
    **Phase:** 01-move-0-prep-liveness-prep
    **Reviewer:** Claude (autonomous Wave C synthesis per CONTEXT.md D-15)
    **Audit evidence:** `artifacts/sub-01/rubric_gate_results.txt`

    ---

    ## 1. Hard rubric gates (7 of 7)

    Per CONTEXT.md `<rubric>` section. Any failure blocks the phase and postpones the 2026-04-18 Step 4B regret check per LIV-03.

    | # | Gate | Command | Result | Status |
    |---|------|---------|--------|--------|
    | 1 | Protected paths clean | `bash scripts/red-team-hybrid/check_protected_paths.sh` | rc=<0> | <PASS|FAIL> |
    | 2 | Freshness all 4 collectors FRESH | `python scripts/red-team-hybrid/freshness_watchdog.py --json` | rc=<0>, status=<OK> | <PASS|FAIL> |
    | 3 | R20 row in risk register | `grep "R20" 10-risk-register.md` | <2 matches in tables; severity 25 Showstopper> | <PASS|FAIL> |
    | 4 | 14-step4b-preconditions.md complete | `test -f` + `grep "^## "` | <exists, 8+ sections, LIV-03 + GOV-03 cited> | <PASS|FAIL> |
    | 5 | Keep-alive installer + ≥2 runs | `test -f` + `ls artifacts/keepalive/*.json` | <installer present, N evidence files OR DEFERRED note> | <PASS|FAIL|DEFERRED> |
    | 6 | Framing correction callout | `grep "Framing Correction (2026-04-08)" 00-strategy.md` | <1 match> | <PASS|FAIL> |
    | 7 | 9% withdrawal complete | `grep "Withdrawn 2026-04-08 (GOV-01)"` + §11 rewritten | <both files caveated; §11 new heading present> | <PASS|FAIL> |

    **Hard gate result: <N of 7 PASS>**

    ### Failures (if any)
    <list each failed hard gate, the reason, and the remediation plan>

    ---

    ## 2. Soft rubric gates (6 of 6)

    Per CONTEXT.md. Failures are documented but do not block the phase per D-03.

    | # | Gate | Command | Actual | Target | Status |
    |---|------|---------|--------|--------|--------|
    | 8 | Track B ≥15 rows | row count | <N> | 15 (target 30) | <PASS|FAIL> |
    | 9 | Founder watchlist ≥30 rows | row count | <N> | 30 (target 50) | <PASS|FAIL> |
    | 10 | Holdout split committed | `ls data/shadow/holdout_split/*.csv` | <files> | exists | <PASS|FAIL> |
    | 11 | Track D design exists with 5 answers | `grep "^## [1-5]"` + TBD count | <5 sections, 0 TBDs> | 5/5 + 0 TBD | <PASS|FAIL> |
    | 12 | STATE.md day-by-day gates | `grep "Phase 1"` | <N entries> | ≥3 entries | <PASS|FAIL> |
    | 13 | Phase 1→2 handoff inputs (D-31) | per-input check | <all 4 present> | 4/4 | <PASS|FAIL> |

    **Soft gate result: <N of 6 PASS>**

    ### Soft gate shortfalls (carry to Phase 2 day 1)
    <list each shortfall + Phase 2 day 1 carry-over plan per D-03>

    ---

    ## 3. SUB-01 Move 0 Charter Deliverables Matrix (D1-D12)

    Per `docs/plans/2026-04-06-red-team-hybrid/01-move-0-charter.md` §3. The 80% rule applies — each deliverable must be at the 80%+ level. Verify file existence + smoke check that the file is substantive (not a stub).

    | # | Charter Deliverable | Location | Phase 1 Action | Status |
    |---|---------------------|----------|----------------|--------|
    | D1 | Strategy charter (renamed) | `00-strategy.md` | GOV-04 added Framing Correction callout (plan 01-02) | <PASS|FAIL> |
    | D2 | Move 0 charter | `01-move-0-charter.md` | unchanged, exists | <PASS|FAIL> |
    | D3 | Bounded-context map | `02-bounded-context-map.md` | unchanged, exists (80% rule applies) | <PASS|FAIL> |
    | D4 | Dead-letter contract | `03-dead-letter-contract.md` | unchanged, exists | <PASS|FAIL> |
    | D5 | LLM failure mode decision | `04-llm-failure-mode.md` | unchanged, exists | <PASS|FAIL> |
    | D6 | Hold-out cohort split design | `05-holdout-cohort-design.md` | GOV-01 caveat at line 44 (plan 01-03) | <PASS|FAIL> |
    | D7 | Tier-2 recall eval design | `06-tier-2-recall-eval.md` | GOV-01 caveat at line 74 + §11 REWRITTEN (plan 01-03) | <PASS|FAIL> |
    | D8 | Collector audit catalog | `07-collector-audit.md` | unchanged, exists | <PASS|FAIL> |
    | D9 | Track B labelling sprint plan | `08-track-b-labelling.md` | doc unchanged; REC-01 shipped seeded CSV (plan 01-08) | <PASS|FAIL> |
    | D10 | Track E founder watchlist | `09-track-e-watchlist.md` + CSV | doc unchanged; REC-04 shipped seeded CSV (plan 01-09) | <PASS|FAIL> |
    | D11 | Risk register snapshot | `10-risk-register.md` | LIV-11/GOV-02 added R20 row (plan 01-01) | <PASS|FAIL> |
    | D12 | Protected-paths verifier | `scripts/red-team-hybrid/check_protected_paths.sh` | unchanged, runs clean | <PASS|FAIL> |

    ### New Phase 1 deliverables (beyond charter D1-D12)

    | Item | Location | Plan | Status |
    |------|----------|------|--------|
    | Freshness watchdog | `scripts/red-team-hybrid/freshness_watchdog.py` | shipped pre-Phase-1 in commit `4efe8cf` (LIV-02) | <PASS> |
    | Step 4B preconditions contract | `docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` | 01-04 (LIV-03 + GOV-03) | <PASS|FAIL> |
    | Track D design doc | `docs/plans/2026-04-06-red-team-hybrid/13-track-d-design.md` | 01-07 (REC-03) | <PASS|FAIL> |
    | Track B candidate seed | `data/shadow/track_b_episodes.csv` | 01-08 (REC-01) | <PASS|FAIL> |
    | Track E founder seed | `scripts/data/founder_watchlist_manual_seed.csv` + `data/shadow/founder_watchlist.csv` | 01-09 (REC-04) | <PASS|FAIL> |
    | Track C hold-out split | `data/shadow/holdout_split/episodes_v1.csv` | 01-06 (REC-02) | <PASS|FAIL> |
    | Reputation stub | `data/shadow/founder_reputation_stub.csv` | 01-09 (REC-04 / D-10 V2-05 stub) | <PASS|FAIL> |
    | Keep-alive installer | `scripts/red-team-hybrid/install_keepalive_task.ps1` + `artifacts/keepalive/` | 01-05 (D-22 R19 root cause fix) | <PASS|FAIL|DEFERRED> |

    ### Schema migration notes (Phase 2 awareness)

    - **`data/shadow/track_b_episodes.csv`** schema migrated from Phase 0 episode-level (`episode_id,canonical_key,episode_start,...`) to D-05 per-signal-candidate (`signal_id,source_api,canonical_key,...`). Pre-migration header captured at `artifacts/rec-01/pre_migration_header.txt`. Phase 2 episode-level rollup (if needed) goes to a separate file.
    - **`data/shadow/founder_watchlist.csv`** populator schema unchanged. Claude-extracted rows are tagged via `founder_id=claude_NNN` prefix; analyst rows use `founder_id=manual_NNN`. Both have `source=manual_seed` from the populator.

    ---

    ## 4. REQ Traceability (11 of 11)

    All Phase 1 REQs must be traced to a specific commit + file.

    | REQ | Description | Plan | Commit | File(s) | Status |
    |-----|-------------|------|--------|---------|--------|
    | LIV-03 | Freshness precondition for Step 4B regret check | 01-04 | <SHA> | `14-step4b-preconditions.md` | <PASS|FAIL> |
    | LIV-11 | R20 (Analyst abandonment) in risk register | 01-01 | <SHA> | `10-risk-register.md` | <PASS|FAIL> |
    | GOV-01 | Withdraw 9% precision claim | 01-03 | <SHA> | `05-holdout-cohort-design.md`, `06-tier-2-recall-eval.md` (lines 44, 74, §11) | <PASS|FAIL> |
    | GOV-02 | R20 governance traceability | 01-01 | <SHA> | `10-risk-register.md` (same edit as LIV-11) | <PASS|FAIL> |
    | GOV-03 | Freshness precondition contract for ALL governance gates | 01-04 | <SHA> | `14-step4b-preconditions.md` (co-located with LIV-03) | <PASS|FAIL> |
    | GOV-04 | Framing correction in 00-strategy.md | 01-02 | <SHA> | `00-strategy.md` | <PASS|FAIL> |
    | SUB-01 | Move 0 charter deliverables verification | 01-10 (this plan) | <SHA> | this VERIFICATION.md | <PASS|FAIL> |
    | REC-01 | Track B 30-episode seed (DB-mined) | 01-08 | <SHA> | `mine_track_b_candidates.py`, `track_b_episodes.csv` | <PASS|FAIL> |
    | REC-02 | Track C hold-out cohort split | 01-06 | <SHA> | `build_holdout_split.py`, `holdout_split/episodes_v1.csv` | <PASS|FAIL> |
    | REC-03 | Track D design only | 01-07 | <SHA> | `13-track-d-design.md` | <PASS|FAIL> |
    | REC-04 | Track E founder watchlist seed | 01-09 | <SHA> | `extract_founder_candidates.py`, `founder_watchlist.csv`, `founder_reputation_stub.csv` | <PASS|FAIL> |

    **REQ traceability result: <N of 11 traced>**

    Note: LIV-01 (R19 manual restart) and LIV-02 (freshness watchdog script) were already shipped in commit `4efe8cf` before Phase 1 planning began. Those REQs are not in Phase 1's scope but are referenced as prior context.

    ---

    ## 5. Phase 1 → Phase 2 handoff inputs (D-31)

    Phase 2 (Move 0.5 Liveness Restoration) needs these structured inputs from Phase 1:

    | # | Input | Path | Schema | Status |
    |---|-------|------|--------|--------|
    | 1 | Freshness JSON | `python scripts/red-team-hybrid/freshness_watchdog.py --json` | `{checked_at, threshold_hours, exit_code, status, collectors[], failures[]}` | <PASS|FAIL> |
    | 2 | Track B label progress | `data/shadow/track_b_episodes.csv` | D-05 per-signal schema | <PASS|FAIL> |
    | 3 | Founder watchlist count | `data/shadow/founder_watchlist.csv` | populator 7-column schema | <PASS|FAIL> |
    | 4 | Governance gate contract | `docs/plans/2026-04-06-red-team-hybrid/14-step4b-preconditions.md` | docs § 1-8 | <PASS|FAIL> |

    All four inputs verified present with expected schemas. Phase 2 day 1 can consume these without re-discovery.

    ---

    ## 6. Threat-model verification (cross-plan)

    Per CONTEXT.md `<security_enforcement>` and the threat models embedded in each plan:

    - **Protected paths:** zero touches to `collectors/`, `workflows/`, `governance/`, `monitoring/`, `connectors/`, `storage/migrations/` across all 9 prior plan commits. Verified via hard gate 1.
    - **Read-only DB access:** every Phase 1 script that touches `signals.db` uses `sqlite3.connect(f"file:{db_path}?mode=ro", uri=True)`. Verified by `grep -rn "mode=ro" scripts/red-team-hybrid/` returning all the new scripts.
    - **No new secrets:** no new env vars, no new API keys, no `.env` modifications.
    - **Supply chain:** new scripts are stdlib-only Python + one PowerShell installer using only Windows built-in cmdlets. No `pip install`, no `Install-Module`.
    - **LinkedIn hard constraint (D-11):** zero LinkedIn URLs in `data/shadow/founder_watchlist.csv` or `scripts/data/founder_watchlist_manual_seed.csv`. Verified by `grep -E ",https?://[^,]*linkedin"` returning zero matches.

    ---

    ## 7. Overall pass condition

    Per CONTEXT.md rubric: **all 7 hard gates must be PASS**. Soft gate shortfalls are tolerated if documented in this VERIFICATION.md with explicit reason and carry-over to Phase 2.

    **Phase 1 result: <PASS|BLOCK>**

    <If PASS:>
    - All 7 hard gates green
    - All 11 REQs traced to commits
    - All 12 charter deliverables (D1-D12) at 80%+ level
    - Phase 1 → Phase 2 handoff inputs ready
    - Phase 1 closes; the 2026-04-18 Step 4B regret check has the substrate it needs
    - Soft gate shortfalls (if any) carry to Phase 2 day 1 per D-03

    <If BLOCK:>
    - Hard gate(s) failed: <list>
    - Remediation: <fix list>
    - Per LIV-03 escalation: 2026-04-18 Step 4B regret check POSTPONES until remediation

    ---

    ## 8. Audit appendix

    - Full rubric gate command output: `artifacts/sub-01/rubric_gate_results.txt`
    - GOV-01 wider-grep sweep: `artifacts/gov-01/grep_9pct_sweep.txt`
    - Track B pre-migration header: `artifacts/rec-01/pre_migration_header.txt`
    - Founder candidates buffer: `artifacts/rec-04/founder_candidates_raw.csv`
    - Per-plan SUMMARY files: `.planning/phases/01-move-0-prep-liveness-prep/01-0[1-9]-SUMMARY.md`

    ---

    *Phase: 01-move-0-prep-liveness-prep*
    *Status: <PASS|BLOCK>*
    *Generated by plan 01-10 Wave C synthesis (SUB-01)*
    ```

    Fill in every `<...>` placeholder with actual values from Task 1's `rubric_gate_results.txt` output and from the prior plan SUMMARYs. If a SUMMARY is missing (some plans may not have run yet at execution time), note "MISSING SUMMARY" in the corresponding REQ traceability row.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md && wc -l .planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md` → exit 0
    - `wc -l .planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md` reports at least `200` lines
    - `grep -c "^## 1\. Hard rubric gates" .planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md` returns `1`
    - `grep -c "^## 2\. Soft rubric gates" .planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md` returns `1`
    - `grep -c "^## 3\. SUB-01 Move 0 Charter Deliverables Matrix" .planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md` returns `1`
    - `grep -c "^## 4\. REQ Traceability" .planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md` returns `1`
    - `grep -c "^## 5\. Phase 1 → Phase 2 handoff" .planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md` returns `1`
    - `grep -c "^## 6\. Threat-model verification" .planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md` returns `1`
    - `grep -c "^## 7\. Overall pass condition" .planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md` returns `1`
    - All 11 REQs cited (`grep -c "LIV-03\|LIV-11\|GOV-01\|GOV-02\|GOV-03\|GOV-04\|SUB-01\|REC-01\|REC-02\|REC-03\|REC-04"` returns at least 11)
    - All 12 charter deliverables D1-D12 listed in §3
    - No `<...>` placeholders remain unfilled (`grep -c "^.*<PASS|FAIL.*$" .planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md` returns `0` — every PASS|FAIL has been resolved to a concrete value)
    - `bash scripts/red-team-hybrid/check_protected_paths.sh` rc=0
  </acceptance_criteria>
  <done>VERIFICATION.md exists with all 7 sections, all gates evaluated, all REQs traced, all charter deliverables checked, Phase 2 handoff confirmed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update STATE.md with Phase 1 day-by-day gate evaluations per D-34</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current content; appended to, not overwritten)
    - .planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md (from Task 2)
    - .planning/phases/01-move-0-prep-liveness-prep/1-CONTEXT.md (D-33, D-34 day-by-day kill criteria)
  </read_first>
  <action>
    Append a new section to `.planning/STATE.md` (do NOT overwrite existing content). The section captures the Phase 1 daily gate evaluations per D-34. If the file already has a "Phase 1 daily gate evaluations" section from earlier daily commits, append to it. If not, create it.

    Add this section (anchor at the end of the existing "Active phase" section, before the "Critical context to inherit on session start" section):

    ```markdown

    ## Phase 1 daily gate evaluations (D-34)

    Per CONTEXT.md D-34, executor MUST update this list when claiming a Wave complete. Each entry is a single line.

    - **YYYY-MM-DD day N check:** <Wave A|B|C> <N/M> landed, <on track|behind|blocked>. <details>

    Entries from Wave C synthesis (plan 01-10):
    - **YYYY-MM-DD day N check:** Wave A complete (LIV-11/GOV-02 R20 row, GOV-04 framing correction, GOV-01 9% withdrawal + §11 rewrite, LIV-03/GOV-03 preconditions doc, keep-alive installer). Hard gates 1, 3, 4, 6, 7 PASS.
    - **YYYY-MM-DD day N check:** Wave B complete (REC-02 holdout split, REC-03 Track D design, REC-01 Track B seed, REC-04 Track E seed). Soft gates 8-11 evaluated.
    - **YYYY-MM-DD day N check:** Wave C complete (SUB-01 charter rollup, VERIFICATION.md shipped). Phase 1 result: <PASS|BLOCK>.

    See `.planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md` for full audit.
    ```

    Replace `YYYY-MM-DD` and `day N` with the actual execution date(s). If all three waves landed on the same day (which is plausible given the Wave A Fermi of ~1-2h Claude effort per CONTEXT.md D-02), use a single entry that covers all three waves.

    **Atomic entry preservation:** if the file is being updated incrementally across multiple days (which is the realistic scenario), each daily commit appends one line and Wave C's commit appends the closing summary line.

    Verify the addition:
    ```bash
    grep -c "Phase 1 daily gate evaluations" .planning/STATE.md
    # expect: 1
    ```
  </action>
  <verify>
    <automated>grep -c "Phase 1 daily gate evaluations" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 1 daily gate evaluations" .planning/STATE.md` returns at least `1`
    - `grep -c "Wave A complete" .planning/STATE.md` returns at least `1`
    - `grep -c "Wave B complete" .planning/STATE.md` returns at least `1`
    - `grep -c "Wave C complete" .planning/STATE.md` returns at least `1`
    - `grep -c "1-VERIFICATION.md" .planning/STATE.md` returns at least `1`
    - The pre-existing STATE.md content (Active phase section, Critical context section, etc.) is unchanged — verify with `git diff HEAD~1 -- .planning/STATE.md` showing only additions, no deletions
    - `bash scripts/red-team-hybrid/check_protected_paths.sh` rc=0
  </acceptance_criteria>
  <done>STATE.md augmented with Phase 1 daily gate entries; pre-existing content preserved; D-34 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → git tree | New VERIFICATION.md + augmented STATE.md + new audit-evidence file under `artifacts/sub-01/`. |
| Rubric gate commands → external state | Each gate command reads existing files / runs existing scripts. No write to protected paths. |
| Cross-plan SUMMARY references | This plan reads SUMMARYs from prior plans. If a SUMMARY is missing, the verification table records "MISSING SUMMARY" but does not fabricate values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | VERIFICATION.md fabricated values | mitigate | All gate values come from `artifacts/sub-01/rubric_gate_results.txt` (committed audit trail). Acceptance criteria require zero unfilled `<...>` placeholders. |
| T-10-02 | Repudiation | Soft gate shortfalls hidden | mitigate | §2 of VERIFICATION.md explicitly lists soft gate shortfalls and Phase 2 carry-over per D-03. |
| T-10-03 | Information disclosure | Cross-plan SUMMARYs may contain SHAs | accept | SHAs are not secrets; this is normal git state. |

<data_integrity>
Read-only access to all source files. Writes go to two allowed locations (`.planning/phases/01-...` and `.planning/STATE.md` append). Audit evidence committed to `artifacts/sub-01/`.
</data_integrity>
<supply_chain>
No new executable code; only Markdown verification report + bash gate-runner inline.
</supply_chain>
<protected_paths>
Zero touches to protected paths.
</protected_paths>
<secrets>
N/A.
</secrets>
<mitigations>
- `check_protected_paths.sh` pre-commit guard
- Audit-evidence file committed (`artifacts/sub-01/rubric_gate_results.txt`)
- Acceptance criteria forbid unfilled placeholders
- STATE.md augmented (not overwritten); pre-existing content preservation verified by git diff
</mitigations>
</threat_model>

<verification>
This plan is the meta-verification plan for Phase 1. Its success implies the entire phase passes (or explicitly fails) the rubric.

Verification commands:
```bash
test -f .planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md  # expect: exit 0
test -f artifacts/sub-01/rubric_gate_results.txt  # expect: exit 0
grep -c "Phase 1 daily gate evaluations" .planning/STATE.md  # expect: ≥1
grep -c "^## [1-7]\." .planning/phases/01-move-0-prep-liveness-prep/1-VERIFICATION.md  # expect: 7
bash scripts/red-team-hybrid/check_protected_paths.sh  # expect: rc=0
```

Soft rubric gate 12 satisfied by Task 3.
SUB-01 satisfied by Task 2 (the SUB-01 verification matrix walked end-to-end).
</verification>

<success_criteria>
- VERIFICATION.md exists with all 7 sections + filled values (no placeholders)
- All 7 hard rubric gates evaluated and PASS (or remediation plan documented if any FAIL)
- All 6 soft rubric gates evaluated; shortfalls documented per D-03
- All 11 Phase 1 REQs traced to commits/files
- All 12 charter deliverables D1-D12 confirmed at 80%+ level
- Phase 1 → Phase 2 handoff inputs (D-31) confirmed present with expected schemas
- STATE.md augmented with Phase 1 daily gate evaluations per D-34
- SUB-01 traced to this plan's commit
- Audit evidence committed to `artifacts/sub-01/rubric_gate_results.txt`
</success_criteria>

<output>
After completion, create `.planning/phases/01-move-0-prep-liveness-prep/01-10-SUMMARY.md` capturing:
- Commit SHA(s)
- Final Phase 1 result (PASS / BLOCK)
- N of 7 hard gates passed
- N of 6 soft gates passed
- N of 11 REQs traced
- N of 12 charter deliverables verified
- Any soft gate shortfalls + carry-over plan
- Confirmation Phase 1 → Phase 2 handoff is ready
</output>
</content>
</invoke>
# Incidents

Operational incident records live here when an issue needs local, auditable context before or alongside GitHub discussion.

Create a new artifact with:

```bash
python scripts/create_doc_artifact.py incident "Short Title"
```

# Q-25 Promotion Readiness Check — 2026-05-16

> Auto-generated by readiness-check agent. Originating claim: 2026-05-02.
> Read-only pass over: `api/routers/merge_review.py`, `api/models/merge.py`,
> `storage/merge_cascade.py`, `tests/api/test_merge_write_endpoints.py`.

---

## R19 status

**MERGED — YES**

| Field | Value |
|---|---|
| Commit hash | `51d379d4d684c68e71d02298a05064eb2b725ebd` (merge commit) |
| Merge message | `Merge pull request #145 from nikhillinit/chore/r19-recovery-evidence` |
| PR title | `chore(r19): recovery evidence + closure (R19 closed)` |
| Merged date | 2026-05-05 |
| Branch | `chore/r19-recovery-evidence` (no longer listed in merged branches — already cleaned up, which is expected) |

R19 landed to main three days after the originating claim date.

---

## Code presence

### merge_entities import

**NOT IMPORTED — NO**

Checked via `grep -n "from storage.entity_identity_store import" api/routers/merge_review.py`.
No match. The only lazy storage imports inside `apply_merge_proposal` (lines 655–657) are:

```
line 655: from storage.audit_events import record_event_from_context
line 656: from storage.merge_cascade import cascade_merge
line 657: from storage.merge_rollback import compute_entity_fingerprint
```

`cascade_merge` is still the sole merge primitive invoked at line 734. Neither
`merge_entities` nor `_merge_entities_internal` appears anywhere in
`api/routers/merge_review.py` or `storage/merge_cascade.py`.

The authoritative writer to `entity_migrations`
(`_merge_entities_internal` at `storage/entity_identity_store.py:519–591`) is
therefore **never called** through the API path.

### Propose-time validator (ProposeRequest)

**ABSENT — NO**

Checked via `grep -n "field_validator|model_validator|@validator|lex_min|resolve_root|resolve_entity_root"` across both files. No match.

`ProposeRequest` at `api/models/merge.py:98–104`:

```python
class ProposeRequest(BaseModel):
    suggestion_id: int   = Field(...)
    winner_company_id: str = Field(...)
    loser_company_id: str  = Field(...)
    reason: Optional[str]  = Field(default=None, ...)
```

No `field_validator` or `model_validator` enforces identity-ordering or
canonical-root resolution at propose time. The raw caller-supplied
`winner_company_id` / `loser_company_id` strings are persisted at
`merge_review.py:541–542` without any validation.

### Composite code verdict

**ABSENT**

Both required code changes — (1) routing `apply_merge_proposal` through
`merge_entities` / `_merge_entities_internal` to write `entity_migrations`, and
(2) a propose-time validator on `ProposeRequest` — are absent.

---

## Test presence

File inspected: `tests/api/test_merge_write_endpoints.py` (660 lines).

Existing test classes: `TestMergePropose`, `TestMergeApprove`, `TestMergeApply`,
`TestMergeRollback`, `TestListProposals`.

| Item | Present | Line ref |
|---|---|---|
| `class TestMergeDirectionalInvariants` | **NO** | — |
| `test_apply_does_not_force_wrong_winner_on_reversed_caller_input` | **NO** | — |
| `test_apply_writes_entity_migrations_row_for_lineage_audit` | **NO** | — |
| `test_rollback_report_entity_migration_deleted_matches_apply_writes` | **NO** | — |

None of the three required test methods exist in the file.

---

## Test execution

**NOT RUN**

Reason: environment setup failed (module `jose` unavailable — `python-jose` not
installable due to `cffi` backend crash in this container). Smoke test
`python -c "from api.routers import merge_review"` exited with
`ModuleNotFoundError: No module named 'jose'`.

Additionally, all three prerequisite conditions for running the pytest target
were unmet independently of the env issue:

- `merge_entities` not imported ✗
- Propose validator absent ✗
- `TestMergeDirectionalInvariants` class absent ✗
- All three test methods absent ✗

Verdict: **not_run_due_to_install_failure** (and not_run_due_to_missing_tests)

---

## Verdict

**NOT-READY**

### What is missing

1. **`merge_entities` / `_merge_entities_internal` not called by the API path.**
   `apply_merge_proposal` calls `cascade_merge` (directional, no
   `entity_migrations` write) and never reaches the authoritative identity-merge
   primitive. `entity_migrations` rows are therefore never written when the API
   apply path executes.

2. **No propose-time validator on `ProposeRequest`.**
   `winner_company_id` and `loser_company_id` are accepted verbatim from the
   caller, persisted at lines 541–542, and re-read by `apply_merge_proposal` at
   lines 682–683. A reversed or unresolved caller input propagates silently into
   the cascade.

3. **`class TestMergeDirectionalInvariants` is absent** from
   `tests/api/test_merge_write_endpoints.py`.

4. **All three required test methods are absent:**
   - `test_apply_does_not_force_wrong_winner_on_reversed_caller_input`
   - `test_apply_writes_entity_migrations_row_for_lineage_audit`
   - `test_rollback_report_entity_migration_deleted_matches_apply_writes`

### Recommended next step

Operator's call. The delta from the verified claim (2026-05-02) is:

- R19 did merge (positive), but it did not close the code gap described in Q-25.
- Code and test gaps are identical to the initial-state description: no progress
  on items 1–4 above.

Do NOT open a fix PR from this agent. Suggested operator actions:

1. Decide whether Q-25 stays open or is descoped.
2. If kept open, author a PR that:
   - Routes `apply_merge_proposal` through `_merge_entities_internal` (or wraps
     `cascade_merge` to call it), ensuring `entity_migrations` rows are written.
   - Adds a `field_validator` or `model_validator` to `ProposeRequest` that
     resolves/validates identity ordering before persistence.
   - Adds `class TestMergeDirectionalInvariants` with the three specified test
     methods as the acceptance gate.
3. Re-run this readiness check after the PR merges to main.
4. Only after GREEN test verdict: update wiki/decisions/041-… and mark Q-25
   adopted in ADR follow-up queue (vault is local-only; no automated path from
   this repo).
